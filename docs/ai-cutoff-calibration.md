# BTX 日末截断估值校准审计

当前黑方联合搜索到达日末、且尚未出现真实胜负或最终猜测时，使用单日局面启发分。该分数用于候选排序，没有被训练成胜率。审计入口：

```powershell
python -m benchmarks.ai_cutoff_calibration --games 4 --seed 0 --strategy fixed --protagonist-nodes 8 --protagonist-ms 1000 --exact-penultimate
```

采集每次 `day_ended` 后进入下一天的非终局状态；每条记录保存红方日末分、硬角色域熵、轮回／日期。对局结束后标注「本轮存活直接赢」「最终猜测赢／输」等终局类型。每局的全部日末记录总权重相同，防止长局因记录更多而主导 AUC。倒数第二天可选运行红方的完整身份 MAP 求解，记录当时答对人数与耗时。`--json` 输出逐局、逐日数据。测试用的对手、种子和预算须和结果一起保存。

2026-09-24 首轮固定策略黑方数据：BTX02、BTX09 标准版各 seed 0–3；粒子主人公每次 8 节点／1 秒。每剧本均有 2 局存活直接红胜、2 局终猜黑胜，合计 80 个日末截断点。按整局红胜的匹配权重 AUC：BTX02 日末分 **0.660**、负硬域熵 **0.278**；BTX09 日末分 **0.457**、负硬域熵 **0.530**；合并后日末分 **0.558**、负硬域熵 **0.443**。在现有样本上加 `-0.02 × 熵` 后的合并 AUC 为 **0.558**，没有可见增益。倒数第二天 20 个完整身份 MAP 探针的最大耗时约 **1.08 秒**；其答对比例对“整局红胜”的 AUC 为 **0.206**。此反向关系来自两类不同的胜利路径，不能解读成“了解更多身份降低红方胜率”。

解释：一个红方仍不知道多数身份的局面，后续可以直接存活到最后一天并获胜；一个反复失败、积累了较多证据的局面，即使终猜多答对几个角色也可能整体失败。局面压力和终猜信息分别描述不同风险。若目标是整局胜率，估值应分别预测「在剩余轮回中直接存活获胜」和「走到最终猜测后整套身份全部猜对」，再按到达各终局的概率组合。要先用更多剧本、不同强度黑方和独立种子校准这些条件概率；不能把目前的启发分或身份答对比例线性映射为胜率。

目前生产搜索只在真实模拟进入 `final_guess` 时以实际整套猜测计分；早期仍保留原日末启发分和可关闭的小幅熵排序项。首轮样本不足以支持修改生产权重。下一步应收集黑方混合策略与不同轮回数的数据，按终局路径分层，并针对同一黑方根候选做后续策略的成对延伸，减少实际对局轨迹与反事实候选之间的分布偏差。

同预算的信念联合黑方补充样本：BTX02、BTX09 标准版各 seed 0–2，黑方每步 16 节点／1 秒，红方仍 8 节点／1 秒；六局全为终猜失败的黑胜，分别得到 13、36 个日末点。该对手更强，但没有红胜样本，AUC 应报告为不可估计，不能把全黑胜解释为估值已被校准。后续要纳入其他黑方风格或扩大红方预算，产生两种结果后再做强黑方下的辨别测试。

## 双路径离线原型

`--path-cv` 增加三段条件概率：直接存活获胜、未直接获胜后进入终猜、进入终猜后整套身份猜对。组合的整局红胜概率为 `P(直接赢) + [1-P(直接赢)] × P(进入终猜 | 未直接赢) × P(终猜全对 | 进入终猜)`。前两段使用日末分与局数／日期进度，终猜段使用公开硬身份域熵与进度。每局记录权重之和为 1；同一 seed 的所有剧本、策略与日期一同留出，用剩余 seed 拟合，再与单头胜率模型、常数先验比较 Brier 分数。原型仅存在于 `benchmarks/`，没有接入对局搜索。

复现命令：

```powershell
python -m benchmarks.ai_cutoff_calibration --scenario official-btx-02-traditional-ensemble-murder-easy --scenario official-btx-09-those-with-antibodies-easy --games 3 --strategy fixed --protagonist-nodes 8 --protagonist-ms 500 --path-cv
```

2026-09-24 探索性运行：6 局、88 个日末点，3 局直接存活红胜、3 局终猜黑胜，**没有终猜红胜**。留出 seed 的 Brier：双路径 **0.439**，单头 **0.465**，常数先验 **0.380**；双路径 AUC **0.049**。由于仅 3 个 seed、终猜正例为零，且该次运行与全量测试共享 CPU、限时搜索轨迹出现波动，这些数字只用于暴露样本不足与校准失败，不能据此宣布某模型更强。倒数第二天的身份正确比例即使提高，也不等同于「整套全对」概率。

下一步先构造覆盖两类红胜、终猜黑胜与其他黑胜的独立样本：补充多种黑方回复风格、红方预算和标准／Easy 轮回；基准顺序运行以减少限时漂移。再按剧本和 seed 双重留出验证，比较概率校准和真实黑方根候选排序。只有双路径持续优于单头及常数基线，且终猜分支拥有足够正反例，才考虑把它接入黑方搜索。

## 固定工作量与完整轨迹复现（2026-09-26）

校准数据采集可加 `--fixed-work`：红黑搜索的墙钟截止均设为 `None`，节点／候选上限、粒子数和策略采样规则仍按原配置运行。这个模式用于复现和生成标签，不用于宣称等时间棋力；不同算法、不同局面的节点成本仍然不同。完整对局耗时继续记录。较大的配置可能耗时很久，最终猜测的精确求解也独立于行动节点预算。

`--repeat-check` 顺序重跑每局，比较全部结构化决策的 SHA-256 和胜方；摘要涵盖出牌、能力与事件选择、终猜等行动，排除耗时信息。出现差异时返回退出码 1，并保留两次摘要和耗时。`--output` 保存完整 JSON（含预算、逐局摘要与逐日日末标签），建议放在 Git 忽略的 `references/` 下。`--protagonist-strategy` 可选粒子版、公开风险防守、公开防守及基础版，用于后续策略分布对照。

```powershell
python -m benchmarks.ai_cutoff_calibration --scenario official-btx-09-those-with-antibodies --games 2 --seed 0 --strategy fixed --protagonist-nodes 2 --fixed-work --repeat-check --path-cv --output references/ai-calibration/btx09-fixed-work.json
```

首轮验证：固定黑方、粒子红方 2 个候选节点、日末 horizon。BTX02 标准版 seed 0、BTX09 标准版 seed 0/1 的完整重复轨迹全部相同（3/3），首次对局分别约 10.3、5.8、7.0 秒。BTX02 在 `PYTHONHASHSEED=1/2` 的独立进程中也得到同一摘要 `f49b9b926df9…`。三个种子均为终猜黑胜，仍缺终猜红胜与这批预算下的直接红胜；本轮不修改生产估值。

下一切片先提高固定工作量红方预算并扩充剧本／轮回数，以保存完整数据的方式收集不同终局路径。确认样本覆盖后，再进行模型校准与跨剧本留出；等墙钟棋力评测继续使用原限时模式，并独立报告实际耗时和轨迹波动。

## 全 FS/BTX 分批采集

`benchmarks.ai_calibration_matrix` 自动发现所有已录入 FS/BTX 官方剧本及标准／Easy／Very Easy 版本。当前为 13 个基础剧本、25 个难度版本。所有子进程串行运行，统一关闭搜索墙钟截止。每局完成后原子保存 JSON；失败局不写成完成检查点。`--max-jobs` 限制本次新增对局数，`--resume` 继续相同矩阵。代码与实际剧本内容都参与配置指纹，修改策略、预算或剧本后会建立独立目录，避免混用不兼容样本；新录入剧本通过新的矩阵自动纳入。

```powershell
# 首轮低预算覆盖；可加 --max-jobs 5 分批运行。
python -m benchmarks.ai_calibration_matrix --output-dir references/ai-calibration/2026-09-26 --protagonist-nodes 2 --games 1
# 继续相同配置，已完成对局不重跑。
python -m benchmarks.ai_calibration_matrix --output-dir references/ai-calibration/2026-09-26 --protagonist-nodes 2 --games 1 --resume
# 下一档：多个预算和共同种子，各预算分别汇总，避免混在一起拟合。
python -m benchmarks.ai_calibration_matrix --output-dir references/ai-calibration/expanded --protagonist-nodes 2 --protagonist-nodes 8 --games 3 --max-jobs 5
```

每个 `run-<指纹>/` 中保留 `manifest.json`、逐局报告和 `summary.json`。汇总按规则集及红方节点预算分组，报告终局路径计数、日末诊断和 BTX 留出 seed 校准。FS 没有最终猜测，独立汇总，不拟合 BTX 双路径模型。单种子覆盖只用于验证入口和寻找终局案例；增加独立种子后才比较估值或棋力。数据和包含隐藏设置的 manifest 均为离线开发资料，不进入玩家界面。

2026-09-26 首轮全部版本覆盖已完成，数据指纹 `e9e12f9863fa9ceb`：固定黑方、粒子红方、2 节点、日末 horizon、seed 0，25/25 对局正常结束。FS 标准版红胜 **1/2**，包含 Easy 为 **2/4**；BTX 标准版红胜 **6/11**，包含 Easy / Very Easy 为 **11/21**。BTX 的其余 **10 局均为终猜失败**，猜对合计 **54/94**，仍无终猜红胜。共采集 FS 36、BTX 285 个日末记录；只有一个 seed，留出校准明确报告不可进行。难度版本共享秘密和种子，这 25 局不能当作 25 个独立样本估计胜率。

对局耗时合计约 **229 秒**，最慢的 BTX08 Easy 约 **70.5 秒**；未据此定位具体瓶颈，后续需拆分证据维护、行动搜索与终猜求解耗时。相同配置的 `--resume` 已验证完成数仍为 25、新增数为 0，不重新运行子进程。下一批先把红方提高到 8 节点并覆盖同一组版本，再补充 seed 1/2；仍按难度和预算保留各自数据，重点寻找终猜红胜并审计 FS02 等稳定失败案例。

### 8 候选档与世界覆盖审计

相同固定黑方、seed 0、日末 horizon 的 8 节点档全部 25 个版本已完成，指纹 `da2c166fc7d187ad`。在粒子集成规划器中，节点预算主要限制三牌组合候选数量；本次 2→8 的身份／事件／暗牌组合世界数都仍为 **4**。这是固定工作量对照，不能称作等墙钟棋力比较。

| 范围（含难度版本） | 2 候选红胜 | 8 候选红胜 |
| --- | ---: | ---: |
| BTX02 | 0/3 | 3/3 |
| BTX04 | 0/2 | 2/2 |
| BTX08 | 0/2 | 2/2 |
| FS01 | 2/2 | 0/2 |
| FS02 | 0/2 | 2/2 |
| BTX09、BTX11 | 0/3 | 0/3 |
| 其余版本 | 11/11 | 11/11 |

FS 标准版仍为 **1/2**、所有版本 **2/4**，但胜负剧本交换；BTX 标准版为 **9/11**、所有版本 **18/21**。总配对为两档都胜 11、8 档独胜 9、2 档独胜 2、都负 3。BTX09 两档仍均为 7/10，BTX11 为 6/11，终猜合计 **20/31**，没有终猜红胜。8 档总对局耗时约 **161.6 秒**、单局最大 **15.3 秒**；耗时降低主要伴随多组剧本直接存活，不能推断单次搜索更便宜。仅 seed 0，不进行留出 seed 拟合。

FS01 定点回放：两档第一轮均在第 2 天谋杀事件中失去女学生，硬 witness 正确锁定其关键人物身份；真实脚本仍通过全部硬约束。第二轮的四个世界中，女学生全为关键人物，但例如真实的社员杀手身份未出现在该批角色样本中，整套真值因而不在这批世界内。8 档包含 2 档的移动防御候选；第 2 天选中的「女学生禁止密谋＋医生友好＋社员降不安」在样本中被计为 4/4 存活，真实局面仍在谋杀事件失败。该候选比采样模型中的移动防御更安全，说明增加候选并不能补偿错误／不足的风险世界覆盖；目前没有据此认定行动结算错误，也不修剪这个合法候选。

新增 `--protagonist-particles` 可独立控制组合世界数，贯通单剧本与全矩阵入口并写入配置／预算；默认公式未改。FS01 标准版 seed 0、8 候选的定点消融：4 世界负（约 1.3 秒）、16 世界负（4.2 秒）、32 世界胜（11.6 秒）。增加世界数同时改变身份、当事人、暗牌抽样，不能只凭这一局认定某个身份已被正确抽到或默认值应升到 32。

```powershell
python -m benchmarks.ai_calibration_matrix --output-dir references/ai-calibration/world-coverage --protagonist-nodes 8 --protagonist-particles 32 --games 1 --max-jobs 5
```

下一步先对全 FS/BTX 进行候选数×世界数的独立对照，再增加共同 seed 1/2；保留 FS01 反向样本与 BTX09/11 终猜失败样本。固定黑方在本档 BTX 偏弱，后续加入已有信念联合黑方，避免只采集容易直接存活的局面。终猜正例仍缺失，双路径模型继续保持离线。新参数接入后源码指纹变化，新试验独立建档，旧数据目录完整保留。
