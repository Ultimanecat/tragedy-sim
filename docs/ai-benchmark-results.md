# AI 自对弈基准

本页记录 2026-09-16 的第一版可复现对抗基线。对手统一使用 `baseline_protagonist`：它只读公开信息，下一天优先逆转
剧作家上一日成功移动的角色；禁止密谋仅由当日逻辑领队使用；其他行动随机。所有局均正常到达规则终局，没有非法行动或
决策上限失败。

## 主要矩阵

FS 2 个、BTX 11 个已录入剧本，每个剧本使用 seed 0–4。两种 MCTS 的搜索预算均为每次剧作家决策最多 8 个节点、
rollout 深度 8；随机和定式策略不进行树搜索。

| 剧作家策略 | 局数 | 剧作家胜局 | 胜率 | 平均实际决策 | 平均搜索节点/局 | 平均整局耗时 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 随机 | 65 | 5 | 7.7% | 134.2 | 0 | 0.707 秒 |
| 定式 | 65 | 47 | 72.3% | 265.2 | 0 | 2.280 秒 |
| 优化 MCTS | 65 | 58 | 89.2% | 335.6 | 1068.7 | 6.256 秒 |
| 策略 MCTS | 65 | 62 | 95.4% | 312.7 | 996.5 | 6.455 秒 |

按规则集拆分的胜局数：

| 策略 | FS（10 局） | BTX（55 局） |
| --- | ---: | ---: |
| 随机 | 1 | 4 |
| 定式 | 6 | 41 |
| 优化 MCTS | 5 | 53 |
| 策略 MCTS | 8 | 54 |

朴素 MCTS 在相同 `8×8` 预算完成了 FS 的 10 局：1 胜，平均 102.9 个实际决策、582.2 个累计搜索节点、17.483 秒/局。
优化 MCTS 对应 FS 为 5/10，但胜局全部来自 `official-fs-02-prevailing-secrecy`；它在
`official-fs-01-first-script` 上 0/5，说明当前估值仍需要逐剧本校准。

策略 MCTS 是下一独立版本：保留优化版的树结构和场景化估值，但让剧作家的扩展顺序与 rollout 连续推进主规则密谋、
关键人物/杀手阈值、事件当事人不安及关键位置关系；主人公 rollout 仍随机。它把 FS 第一剧本从 0/5 提升到 3/5，
FS 合计 8/10；BTX 为 54/55。总胜率达到 95.4% 后，后续迭代转向增强主人公 AI。

## 公开信息防守主人公

第二个主人公版本 `defensive_protagonist` 在基础约定上增加：降低已公开事件当事人的不安、保护已知关键人物、为公开的
防守型好感能力补足好感。它仍只读取主人公视图。用同一个策略 MCTS、相同 13 个剧本、seed 0–4 和 `8×8` 预算重跑：

| 主人公策略 | 剧作家胜局 | FS | BTX | 平均实际决策 | 平均搜索节点/局 | 平均整局耗时 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 基础干扰 | 62/65（95.4%） | 8/10 | 54/55 | 312.7 | 996.5 | 6.455 秒 |
| 公开信息防守 | 61/65（93.8%） | 7/10 | 54/55 | 426.8 | 1104.2 | 9.152 秒 |

它只减少一个剧作家胜局，却显著延长对局；静态确定事实防守不足以对抗剧本路线策略。下一版本应根据公开揭牌历史推断
剧作家关注目标，在身份或当事人正式公开前分配防守资源。

### 历史风险负基线

`risk_aware_protagonist` 对历次公开揭牌中的剧作家密谋、不安和移动目标做时间衰减统计，再据此分配禁止密谋、禁止移动和
不安 -1。FS 的 10 局预检中，策略剧作家为 10/10；相比基础干扰的 8/10、公开信息防守的 7/10 明显回退。按预设门槛
提前停止，没有继续消耗时间运行 BTX 55 局。该版本作为可复现负基线保留：简单目标频次不是可靠的身份信念，下一步应在
多个观测相容的隐藏世界上聚合行动价值。

### FS/BTX 第一版 ISMCTS 烟测

`ismcts_protagonist` 使用约束生成的隐藏世界、FS/BTX 硬 witness 和根信息集行动聚合。为先验证终局性与 13 个已录入剧本
的兼容性，使用策略 MCTS 剧作家、seed 0、共同 `4×4` 预算各跑一局：

| 主人公策略 | 剧作家胜局 | 平均实际决策 | 平均搜索节点/局 | 平均整局耗时 |
| --- | ---: | ---: | ---: | ---: |
| 基础干扰 | 13/13 | 343.9 | 606.2 | 4.882 秒 |
| 第一版 ISMCTS | 13/13 | 355.7 | 598.5 | 6.551 秒 |

全部对局到达正式终局，ISMCTS 根行动始终属于服务端提供的合法 offer。它略微延长对局，但没有减少该单种子矩阵中的剧作家
胜局，不能宣称棋力提升。复查发现该烟测还混入两个缺陷：公开状态重建保留了采样剧本的 setup 瞬时队列，且固定深度可能在
当天结算完成前估值。更重要的是，当时的基准把真实对局也用 `search_transition` 推进；该快速克隆只保留最后一条事件，导致主人公
在每个决策前丢失此前轮回的公开历史。因此上述 13/13 只能作为旧版工程基线，不能用于判断修复后棋力。当前真实轨迹使用完整
真实 `dispatch`，树内 rollout 才使用精简克隆；状态重建会清除 setup 瞬时队列，并至少推进到当前天日末或立即失败的轮回边界。
搜索仍只覆盖主人公出牌根节点；内部能力/事件选择回退防守策略。完成跨阶段观察日志和持续粒子后，应以相同矩阵重跑多种子对比。

### 重复失败证据的定点回归

用 `official-btx-02-traditional-ensemble-murder`、seed 0、双方共同 `4×4` 预算做定点诊断。修复完整历史后，主人公能看到前两轮
女孩在学生身边死亡并随即失败；软 witness 的最终粒子中，学生的杀人狂概率成为主模态，女孩则集中在关键人物/亲友两种都能
解释死亡后失败的身份。证据仅过滤/加权世界并参与最终猜测，不直接触发移动或禁止移动；同样也不消耗根行动访问次数。

开发过程中曾短暂试验“看到重复死亡就绕过搜索，固定移动/锁人”的协作规则，它能在该 seed 打断一次死亡链并把最终结果从
5/9 提到 6/9，但这混淆了证据层与行动层，现已删除，不能作为当前棋力结果。当前版本也从局面估值中删除了公开知识奖励，
不会为了收集 witness 而主动接受轮回失败。若收集信息未来确实能提高整局最终胜率，应由跨轮回 MCTS 的真实终局回报自然体现。

房间级持续粒子会带来额外墙钟时间，但其观察推进、witness 匹配和重采样属于信念维护成本，与 `SearchBudget` 分开记录。当前
两人局团队 AI 只维护一份共享信念；命令观察已显式区分 `public / partial / hidden`。

删除行动捷径和知识估值奖励后的当前定点结果为：策略剧作家获胜，97 个实际决策，女孩五轮均死亡，最终猜对 4/9（其中仍正确
识别学生=杀人狂、女孩=关键人物），耗时约 40.1 秒。新增分项计时显示：主人公证据/粒子维护约 3.0 秒，主人公行动搜索约
35.6 秒；瓶颈仍主要是每个宽根行动至少一次完整 rollout，而不是搜集证据。后续优化二者必须分别度量，不得用减少 MCTS 根访问
次数来支付证据维护成本。

把 A/B/C 改为一个团队实例后，主人公三张行动牌进一步合并为一个联合节点：`12×12` 预算的定点局耗时降为约 8.5 秒，其中
证据约 2.1 秒、行动搜索约 0.3 秒；但仍为剧作家胜、五轮同一死亡、最终 3/9。把主人公联合节点预算单独提高到 96、深度 16
仍未改变该样本胜负和猜测数。因此本切片确认了团队控制语义与性能改进，尚未证明胜率提升；下一瓶颈是联合组合的候选生成和
跨粒子比较质量，而不是再拆回三个独立代理。

### FS 第一剧本三牌搜索修正（2026-09-19）

在 `official-fs-01-first-script` 上固定策略剧作家 `24×12`、主人公 `96×16`、seed 0–4。旧团队 ISMCTS 是
“第一张搜索、后两张由防守基线补全”，主人公 0/5。新版本先形成有上限的完整三牌候选池，再让每个候选在共同粒子批次上
重复访问；96 次模拟形成 12 个候选、各访问 8 次，主人公 1/5。它解决了旧版 96 次模拟产生 63–93 个组合、
绝大部分仅访问一次的统计缺陷，但尚未通过路线图的 3/5 门槛。另对已公开翻牌与首轮事件当事人进行粒子桥接，
保留晚期 `all_particles_rejected` 作为可审计诊断；这不是完整的跨轮回历史条件重采样。尝试提高软死亡证据权重后
同矩阵回退至 0/5，已撤销该调参。

### FS 第一剧本分维信念与硬身份线索（2026-09-19）

新版团队 AI 将身份/剧本、逐日事件当事人、当天未翻开的暗牌分别建模；FS01 的 6 人身份分配完整枚举，模拟时才临时组合成世界。FS 中若公开日志显示单人死亡紧接轮回失败、且没有正常日末结束事件，则将该角色视作已确定的关键人物；低密谋角色日末与唯一同伴同区死亡时，该同伴视作已确定的杀人狂。seed 1 从第二轮起 24/24 个搜索世界都将女学生判为关键人物；证据维护不占用行动节点预算。

固定剧本 `official-fs-01-first-script`、策略剧作家 `24×12`、seed 0–4：

| 主人公预算 | 组合世界 | 主人公胜局 | 平均整局耗时 |
| --- | ---: | ---: | ---: |
| 96×16 | 24 | 2/5 | 约 18 秒 |
| 384×24 | 64 | 2/5 | 约 42 秒 |

上述结果表明身份信息已经能正确收敛，但搜索仍未充分利用它。逐局观察有关键防守牌过早使用、后续日期无牌可用的情况。扩大节点和世界数并未通过路线图 3/5 门槛，且种子胜负发生波动。384 次模拟下把候选上限从 24 缩到 12 仍为 2/5、平均约 52 秒，已撤销。需要改进跨日规划、候选覆盖和未来对手行动模型。曾试验的重复暗牌偏好、身份后验直接加到出牌排序，以及固定策略剧作家 rollout，均未稳定提升固定种子胜局，已撤销。

### 可选的当日生存优先搜索（2026-09-20）

新增 `ismcts_survival`，保留原 `ismcts` 为独立对照。新模式把 rollout 截在当日日末，先比较候选三牌组合在共同样本中的当日存活率；继续按公开历史揭牌（同一目标／牌面，近期和同日加权）抽取剧作家暗牌，同时保留均匀探索，并在部分样本中强制纳入历史高频牌面。候选池特别包含关键人物的禁止移动、横向移动及移动其他角色改变杀人狂所在地点人数的路线。每个主人公都有自己的禁止移动牌，因此三人合计最多可在一轮中分别使用三次；不可把它当作全队共享的一张牌。

同一 FS01、策略剧作家 `24×12`、主人公 `96×16`、seed 0–4：主人公 **4/5**，平均整局约 **20.6 秒**；旧 `ismcts` 为 **2/5**、约 **18 秒**。seed 2 的第三轮中，主人公使用不同席位的禁止移动应对重复针对女学生的移动，最终获胜。此前仅用“同日历史权重”且没有战术候选的中间试验为 1/5，不能把提升归功于暗牌权重一项。当前每组通常只有 8 次模拟，4/5 仍是小样本结果，不意味着所有合法暗牌下必活；跨日资源规划、BTX 广泛对弈和更严格的敌对抽样尚需继续验证。

### FS 其它剧本对照（2026-09-20）

固定策略剧作家 `24×12`、两种主人公均 `96×16`、相同 seed 0–2；胜局为主人公胜局，时间为**完整对局**平均耗时，不是单次决策延迟。

| FS 剧本 | 普通团队 ISMCTS | 当日生存优先 ISMCTS | 平均耗时：普通 / 生存优先 |
| --- | ---: | ---: | ---: |
| `silent-town-fs`（3 天、3 轮） | 3/3 | 3/3 | 11.3 / 8.5 秒 |
| `official-fs-02-prevailing-secrecy`（5 天、4 轮） | 0/3 | 0/3 | 132.2 / 91.1 秒 |

FS02 的真实隐藏剧本是主规则“守护此地”、子规则“流言四起”，学校密谋 ≥2 会在轮回结束时导致失败；另有第 3 天医院、第 5 天远隔事件。在生存优先版 seed 0 的四次失败中，内部诊断均同时包含关键人物医生死亡与规则 Y 条件成立；**但公开事件顺序均为医生在事件阶段死亡后立即 `loop_lost`，没有正常 `day_ended`**。医生在第 1、3 轮单独死亡后马上失败，现有 FS 硬 witness 能正确推出“医生＝关键人物”。第 2、4 轮医院事件同时造成医生、患者死亡，单凭这次失败不能断言哪个死者是关键人物。主人公并不知道内部诊断中的“规则 Y 条件成立”。

**另有一个独立确认的覆盖缺口**：构造一个仅由学校密谋 ≥2 触发的正常轮回末失败局面，公开视图出现 `loop_lost`，当前编译器却不生成任何主规则 witness。这个缺口并不能解释上面 FS02 的 0/3，因为实际四轮都先发生了医生关键人物的立即失败。后续应保存轮回末公开状态，按所有可能的失败路线构造相容性检验；有死亡等替代解释时不得把“学校密谋 ≥2 且失败”硬判为“守护此地”。本组更直接暴露的是：AI 已能推出医生身份，却仍重复没防住医院／远隔事件，因此首先需要跨日行动规划。FS02 平均 91–132 秒／局也提示长剧本性能需要独立优化。

### FS 已知剧本主人公诊断基线（2026-09-20）

`oracle_cards` 知道剧本及当天剧作家牌面；`oracle_script` 知道剧本，但当天暗牌只按公开目标、可用牌与揭牌历史构造候选。二者共用完整三牌候选与同一引擎的日末结算。固定策略剧作家、seed 0–19、每次主人公决策最多生成 24 个候选三牌组合；表中为**主人公胜局**和完整对局平均耗时。

| FS 剧本 | 明牌胜局 | 暗牌胜局 | 平均耗时：明牌 / 暗牌 |
| --- | ---: | ---: | ---: |
| `silent-town-fs` | 20/20 | 19/20 | 1.0 / 1.7 秒 |
| `official-fs-01-first-script` | 20/20 | 18/20 | 1.4 / 2.5 秒 |
| `official-fs-02-prevailing-secrecy` | 20/20 | 20/20 | 2.1 / 2.6 秒 |
| **合计** | **60/60** | **57/60** | — |

另以低预算策略 MCTS 剧作家（8 节点×12 深度）对 FS01、FS02 各跑 seed 0–1：明牌版分别 2/2、2/2，暗牌版分别 1/2、2/2。样本很小，不应据此推断对强剧作家或混合策略的胜率。FS02 曾因候选漏掉“移开学校邪教徒＋禁止学校密谋”而明牌版 0/5；补入该合法三牌联动并加入规则回归测试后，才得到上表结果。暗牌版还必须主动纳入公开目标处罕见但致命的密谋 +2 场景，否则均匀抽样会漏防。

60/60 证明的是这些测试对局没有击败当前明牌规划器，**并不证明行动处理算法完美**：候选池未穷举，每次只模拟到当日日末，能力选择仍依赖防守策略，后续剧作家行动未被显式预测。下一步应构造小局面的穷举参考值、报告可防守局面的漏解率，并让暗牌版对抗真正会混合出牌的剧作家；通过这些检验后再复用规划器到 ISMCTS。

### FS MCTS 剧作家对已知剧本主人公（2026-09-20）

同一 FS 剧本分别用 seed 0–4；剧作家优化／策略 MCTS 均为每次决策 `24` 节点、rollout 深度 `12`，主人公两版均最多生成 `24` 个三牌候选。表中是**剧作家胜局**，不是主人公胜局。

| 剧本 | 优化 MCTS vs 明牌 | 优化 MCTS vs 暗牌 | 策略 MCTS vs 明牌 | 策略 MCTS vs 暗牌 |
| --- | ---: | ---: | ---: | ---: |
| `silent-town-fs` | 0/5 | 2/5 | 1/5 | 1/5 |
| `official-fs-01-first-script` | 0/5 | 0/5 | 0/5 | 1/5 |
| `official-fs-02-prevailing-secrecy` | 0/5 | 0/5 | 0/5 | 0/5 |
| **合计** | **0/15** | **2/15** | **1/15** | **2/15** |

代表性轨迹：FS02 seed 0 的策略 MCTS 首日打出不安 +1→学生、禁止不安→医生、密谋 +2→学校；暗牌主人公打出禁止密谋→学校及横向移动→学生。学生是学校的邪教徒，移出后不能再让学校忽略禁止密谋，剧作家的主规则路线落空。FS01 seed 1 则相反：剧作家纵向移动关键人物女学生，暗牌主人公没有及时改变她与杀人狂的相对位置，同一死亡路线导致三轮失败；明牌版在相同种子移开杀人狂，最终获胜。

当前 MCTS 逐张搜索剧作家的三张牌。FS02 首日实测约 100 个合法首牌、第二张 81 个、第三张 64 个；24 节点的策略 MCTS 只访问 10 个根首牌。其 rollout 把主人公后续选择主要当随机行动，远弱于被测的三牌联合防守者。这些是后续黑方优先改进的结构性证据，而非仅凭胜率推断。完整对局耗时受轮回长度强烈影响：官方两剧本合并的平均值，优化 MCTS 对明牌／暗牌约 10.5／26.2 秒，策略 MCTS 约 6.8／14.1 秒；优化 MCTS 对暗牌的 FS02 seed 2 单局约 116 秒。不能将这些整局耗时当作单次决策延迟。

预算试验：同样的策略 MCTS 对暗牌版，FS01 seed 0–2 从 24 节点扩到 96 节点，黑方仍为 1/3，但获胜种子从 seed 1 变为 seed 0；96 节点平均整局约 35.3 秒。FS02 首日根节点已访问的首牌从 24 节点时的 10/100 增到 96 节点时的 20/100；96 节点的 FS02 seed 0 单局运行超过两分钟仍未结束，因评测成本停止该附加试验，**不计入胜率表**。单纯扩大节点数既未稳定提升这组三局，也显著增加长局成本；联合三牌行动与对手模型应先于进一步粗放扩预算。

### FS 三牌联合剧作家初版（2026-09-20）

新增独立的 `joint` 剧作家模式：在一天开始时生成至多 24 个合法三牌组合，去除只交换放置顺序的重复组合；每个组合先由真实引擎放置，再由已知剧本且看见暗牌的主人公 AI（12 个候选）联合回应，并结算到当日日末。后两张牌缓存、提交前复核合法性；非放牌阶段仍使用策略 MCTS。它是**有限候选的单日搜索，不是穷举，也不是完整三牌 MCTS**。旧 `strategic` 与 `optimized` 保留为可选对照。

与前表相同的 24×12 黑方、24 候选主人公预算，但仅 seed 0–1 的预检结果如下（剧作家胜局）：

| 剧本 | 联合版 vs 明牌 | 联合版 vs 暗牌 | 平均完整对局耗时：明牌 / 暗牌 |
| --- | ---: | ---: | ---: |
| `silent-town-fs` | 0/2 | 0/2 | 10.1 / 10.9 秒 |
| `official-fs-01-first-script` | 0/2 | 1/2 | 14.9 / 69.2 秒 |
| `official-fs-02-prevailing-secrecy` | 0/2 | 0/2 | 26.2 / 61.8 秒 |

此版本尚未显示稳定棋力收益；FS01 暗牌胜局是小样本，不能证明超过旧策略 MCTS（旧版在 seed 0–4 中 FS01 暗牌为 1/5）。明显的瓶颈是：候选只随机覆盖 24 个三牌组合，回应模型是*明牌*而不是实际暗牌对手，而且只看当天日末、没有预算内跨日规划。后续应增加剧本路线模板与可审计组合覆盖，再对暗牌混合策略建模，并做同预算／同时间的消融。

### FS01/FS02 第二轮定点优化（2026-09-20）

黑方 `joint` 仍保持每次 24 次三牌评估，但用剧本目标／事件生成首批路线候选，把主人公回应改为只知剧本、不知当天暗牌的 `oracle_script`；前列组合再以更大回应预算验证，以压低低预算模型的假阳性。红方两版共用更系统的关键防守候选与跨日压力估值；暗牌版在公开目标为关键人物时另考虑三种移动及密谋 +1/+2，在目标为杀手或主规则地点时考虑密谋 +1/+2。跨轮回重复同一公开目标下的失败出牌受轻微惩罚，但当日存活仍优先。这些都只在 FS 的已知剧本诊断基线上使用。

固定 `official-fs-01-first-script` 与 `official-fs-02-prevailing-secrecy`，各 seed 0–4；剧作家 `24` 次组合评估、非放牌阶段深度 `12`；主人公每天最多 `24` 个三牌候选。胜局是**主人公**胜局：

| 对手剧本 | 明牌红方 | 暗牌红方 | 黑方赢暗牌 |
| --- | ---: | ---: | ---: |
| FS01 | 5/5 | 3/5 | 2/5 |
| FS02 | 4/5 | 5/5 | 0/5 |
| 合计 | **9/10** | **8/10** | **2/10** |

FS01 seed 1、2 的暗牌红方失利并非搜索结算错误：同种子的明牌版获胜。seed 1 还观察到第 1、3 轮因主人公死亡而失败、第 2 轮关键人物死亡；仅把红方候选预算从 24 扩到 48，seed 1、2 仍输。反过来，seed 0 原先会把关键人物留在孤立区域，补入移动场景与次日孤立风险后获胜。FS02 seed 4 的明牌版选“纵向移动学生”后在四轮分别败于医生死亡、医院事件和学校密谋；暗牌版选“横向移动学生”并胜利。曾尝试把暗牌版的稳健计划加入明牌候选并核验当天存活：seed 4 转胜，但原本能赢的 seed 2 转败，故**未保留**。这明确暴露单日日末估值的跨日盲点。黑方确能利用暗牌不确定性，但不能由 2/10 推断稳定真实胜率，也不能由明牌 9/10 推断所有可防守局面零漏解。单次开局黑方决策在本机约 5–8 秒；长剧本完整对局可超过一分钟。下一步应增加跨日搜索、明确混合策略和更广的种子与剧本测试。

### 纠正无效主人公候选后的复测（2026-09-20）

留出 seed 5 揭露候选生成错误：同一天两张“禁止密谋”会在规则引擎中**全局抵消**，而旧规划器仍频繁把两张放进一组三牌；还会选择“友好→地点”“不安→地点”等没有效果的佯攻牌。现在只允许一张禁止密谋，并把除禁止密谋以外的地点牌从主人公候选中排除。这不改变规则引擎，也不限制三名主人公在不同天分别使用自己的禁止牌。FS01 seed 5 的首日明牌组合由无效地点佯攻转为保护关键人物与相关移动；同预算重赛由黑方胜转为红方胜。

按上一节相同预算，把两个官方剧本各扩至 seed 0–7；下表是**修正后的完整重赛**，不能与上一节的中间版本混算：

| 对手剧本 | 明牌红方 | 暗牌红方 | 黑方赢暗牌 |
| --- | ---: | ---: | ---: |
| FS01 | 8/8 | 7/8 | 1/8 |
| FS02 | 8/8 | 6/8 | 2/8 |
| 合计 | **16/16** | **13/16** | **3/16** |

暗牌红方失利的是 FS01 seed 5、FS02 seed 1 和 2；这些种子的明牌红方均获胜。FS02 暗牌的完整对局约 30–193 秒，耗时差异主要来自轮回长度与暗牌情景数。由此只能说当前采样中明牌几乎稳赢、暗牌胜率较高且黑方确能赢部分暗牌；不能证明明牌理论必胜、暗牌对未知对手的真实胜率，也不能证明联合黑方优于旧版策略 MCTS。下一步应检查暗牌输局的局部反事实、跨日规划，以及同墙钟时间的对照。

### 粒子集成主人公：秒级同墙钟初测（2026-09-21）

这是**不知真实剧本**的新版红方与旧“当日生存优先 ISMCTS”的首个成对对照，不应与上面的作弊 Oracle 成绩直接比较。黑方均为联合三牌版，每次搜索配置 24 节点、3 秒软上限；红方均配置 96 节点、3 秒软上限，新版用 12 个分解粒子，旧版按原配置用 24 个。单次日末模拟不可中途打断，因此墙钟上限可能有少量越界。两个官方 FS 剧本各 seed 0–1，串行在同一机器运行。胜局是红方胜局：

| 剧本 | 粒子集成新版 | 旧生存优先版 | 新版／旧版整局均时 |
| --- | ---: | ---: | ---: |
| FS01 | 2/2 | 1/2 | 28.9 / 28.9 秒 |
| FS02 | 1/2 | 0/2 | 99.9 / 95.6 秒 |
| 合计 | **3/4** | **1/4** | — |

实现过程中曾以 32 粒子配 3 秒预算、先提出多组候选再评估，FS01 仅 0/2；缩至 12 粒子但错误地把候选数锁在 24 时，同一初始局面实际只用了约 0.85 秒。去除过早上限后，初始局面 3 秒内完成 80 组三牌／960 次粒子交叉模拟，最终得到上表 FS01 的 2/2。另在同一初始局面以 24 粒子、384 候选、60 秒上限做单决策探针，完成 289 组／6936 次交叉模拟，实际约 30 秒。**这只是计算扩容检查，不是分钟档整局棋力结论。** FS02 seed 0 仍输，且本表每格仅两局；下一步需扩展成对种子、检查失败轨迹并完成分钟档整局对照。

分钟档首个**整局配对**：FS01 seed 1，双方红方均为 384 节点、每次决策 60 秒软上限，黑方仍为 24 节点／3 秒。新版和旧生存优先版都获胜，分别耗时 **285.9 秒／57.4 秒**，各有 152 次实际决策；因此新版在这一局没有体现大预算效率优势。对照秒级同种子（新版胜、旧版负），至少说明旧版会随预算扩展跨过获胜门槛，而不能从秒级两局外推大预算强度排序。FS02 的分钟档及更多成对种子仍未完成，不能把这一局称作一分钟预算的总体胜率。

另对旧版明牌主人公在 `silent-town-fs` seed 4 输给策略 MCTS 的反事实检查发现：已知杀手在首日被放牌时，给其使用禁止密谋可避免早期密谋积累，原先的候选排序却很少纳入此防守。修正候选排序后，同一 seed 4、双方相同预算的重赛由主人公获胜。因此那次失败并非已证实的规则无解；但当前明牌主人公仍是有限候选、单日评估，不能由这一局推断理论必胜。

## BTX 粒子集成第一版：11 剧本完整对局（2026-09-21）

粒子集成主人公现可在 BTX 两人局使用：仍只用主人公可见视图提出并跨粒子评估完整三牌，日末评分不套用 FS 专有压力项；最终猜测直接对完整身份候选按 witness 权重排序，不再从少量抽样的偶然众数或逐角色边际和决定。它仍是**单日规划**，没有把下一轮可能获得的信息计入行动收益，也没有新增 BTX 专项因果 witness。

使用全部 11 个已录入 BTX 官方剧本，每个剧本仅 seed 0；黑方都是优化 MCTS `4×4`，红方两版都是 `8×6`，新粒子集成使用 4 个世界，旧版为“当日生存优先 ISMCTS”。两版均在最终猜测修正后的同一代码上运行。红方胜局分别为**6/11 与 3/11**；新版本额外赢 `official-btx-10-prologue`、`official-btx-06-secret-that-was-kept`、`official-btx-02-traditional-ensemble-murder`，其余 8 个剧本胜负相同。黑方胜局的最终猜测正确人数分别为：

| 剧本 | 粒子集成红方 | 旧生存优先红方 |
| --- | ---: | ---: |
| BTX01 | 3/8 | 0/8 |
| BTX08 | 5/9 | 5/9 |
| BTX03 | 5/9 | 3/9 |
| BTX09 | 4/10 | 4/10 |
| BTX04 | 4/9 | 3/9 |
| BTX10、BTX06、BTX02 | 红方在日内存活并获胜 | 2/9、4/9、5/9 |

这里的“赢”主要仍是**避免轮回失败**，不能把最终猜测 3/8、5/9 等少量样本解释为可靠后验校准。各剧本只跑一个种子，且两版虽然节点数相同，却没有相同的每决策墙钟限制；整局平均耗时约 14.2／18.0 秒还受胜负导致的决策数影响。只能视为 BTX 功能覆盖和定位弱点的第一组配对回归，不是稳定棋力结论。下一步应增加成对种子、秒级／分钟级等墙钟评测，逐条检验 BTX 强制效果、事件及轮回末失败 witness，并把最终猜测正确人数和行动失败原因分开汇总。

复现命令（两版分别将 `--protagonists` 设为 `particle_ensemble`、`ismcts_survival`）：

```powershell
python -m benchmarks.ai_self_play --module BTX --games 1 --nodes 4 --depth 4 --protagonist-nodes 8 --protagonist-depth 6 --strategy optimized --protagonists particle_ensemble --progress
```

### BTX 已知剧本 Oracle 诊断基线

把 FS 的两种 Oracle 扩展到 BTX 后，先发现错误候选曾尝试同日给时间旅行者“友好 +2、再 +1”。规则引擎的合法行动没有包含第二次同目标出牌，实际搜索也没有执行非法行动；错误来自候选模板误把不可行组合当作对策。现在改为合法的跨日阈值计划：友好 0–1 时先 +2，后续日期为 2 时再 +1，并对所有生成三牌断言同阵营目标互异。

全部 11 个 BTX 剧本、seed 0、红方 12 候选；下表累计的是**轮回失败数**，而不是整局胜负。Oracle 已知身份，耗尽轮回后会猜对全部角色，因此两版整局都是 11/11 主人公胜；只报胜率没有诊断意义。

| 黑方 | 明牌 Oracle 累计失败轮回 | 暗牌 Oracle 累计失败轮回 |
| --- | ---: | ---: |
| 固定策略 | **1** | **7** |
| 优化 MCTS `4×4` | **7** | **17** |

明牌版对优化黑方的 7 次失败中，BTX09 占 5 次：行动牌把第 1 天当事人的不安降回 0 后，真实结算中当事人的传谣人能力仍可在剧作家能力阶段给自己不安 +1，从而触发蝴蝶效应及规则 Y。这条路线不能由当天主人公行动牌可靠阻止，最终猜测才是剧本预期出口，不能记为明牌规划器漏解。其余两个失败分布在 BTX01、BTX08，各一个，后续增加种子时再做反事实检查。暗牌版与更强黑方之间的差距说明牌面不确定性确实产生了额外轮回损失。

### BTX 联合 witness 单种子冒烟（2026-09-21）

加入事件时公开快照，以及“契约×关键人物”“定时炸弹×魔女初始区域”“最终日低友好×时间旅行者”的局部联合软因子后，使用与粒子集成第一版相同的命令复跑全部 11 个剧本、seed 0。结果为主人公 **5/11**、剧作家 **6/11**，平均整局约 19.9 秒；失败局最终猜对数依次为 BTX01 1/8、BTX08 5/9、BTX03 3/9、BTX09 6/10、BTX02 5/9、BTX04 4/9。

加入联合因子前的最近一次同配置运行为主人公 6/11、剧作家 5/11，但两个版本的后验变化也改变了后续采样随机序列，单个 seed 不能证明棋力退化或提升。当前结论仅为：新因子确实进入了行动与终局后验，固定权重尚未校准，正确的因果结构不自动等于更强策略。下一步需要为联合软因子提供消融配置，并在相同 seed 0–4、相同墙钟下成对比较；在此之前不继续手调权重。

消融配置现可在同一代码版本中使用 `--disable-joint-witness` 关闭上述三个联合／身份压力通道，同时保留事件快照、病毒当前身份语义和其它 witness。例如：

```bash
python -m benchmarks.ai_self_play --module BTX --games 5 --nodes 4 --depth 4 --protagonist-nodes 8 --protagonist-depth 6 --strategy optimized --protagonists particle_ensemble --disable-joint-witness --progress
```

随后完成全部 11 个 BTX 剧本、seed 0–4 的同代码消融，共 110 局：

| 联合 witness | 主人公胜 | 剧作家胜 | 平均决策数 | 平均整局耗时 |
| --- | ---: | ---: | ---: | ---: |
| 开启 | 28/55 | 27/55 | 343.4 | 22.1 秒 |
| 关闭 | 28/55 | 27/55 | 335.6 | 21.4 秒 |

总体胜负完全相同。开启后 BTX05 的 seed 2/3 胜负互换，BTX03 seed 0 从主人公胜变为剧作家胜，而 BTX04 seed 4 从剧作家胜变为最终猜测 9/9 的主人公胜。对两组都进入最终猜测的相同样本，BTX09 与 BTX04 的正确数增加，BTX01 减少，其余多数相同。

复查终局 trace 时发现 BTX08、BTX11 含有“局外人”，而通用约束采样器曾错误地让局外人占用规则身份槽；实际上其身份应来自“模组存在但所选规则未使用”的额外槽。这使这两个剧本的粒子集合为空并回退到公开防守／全普通人猜测。修复并单独重跑两个剧本的 20 个样本后，胜负仍未变化：BTX08 两组均为主人公 0/5，BTX11 均为 4/5；但 BTX08 已确认每次行动实际使用粒子规划，seed 0 的最终猜测从全普通人 5/9 改为有结构的 6/9。BTX08 五局猜对数为开启 6、6、4、3、4，关闭 4、5、4、4、5。

用修复后样本替换无效样本，所有实际最终猜测合计约为开启 110/255、关闭 94/246；两组到达终局的轨迹仍不完全一致，不能把这个不同分母比例当成严格效果量。

结论：首批联合因子保留了正确的因果结构，也确实能改变局部最终后验，但在当前单日行动规划和低预算下没有带来总体胜率提升。暂不手调固定权重。BTX08 的典型错误已从“采样器失效”收窄为把真实的 `unknown/factor` 组合猜成另一个没有被公开现象排除的规则 X／身份组合；需要主动调查或记录因子能力现象，而非继续增强相同轮回末权重。BTX01 多数只猜对 0–2/8，也更可能缺少事件／身份信息或跨轮回主动调查。

为避免只看逐角色边际误判，终局私有 trace 现额外记录权重最高的 8 套完整候选（规则 Y、两个规则 X、全部初始身份和权重），不进入玩家视图或 replay。BTX08 seed 0 的首轮诊断显示，错误首选为 `sign + lurking + rumor`，真实结构为 `sign + unknown + virus`。随后修正死亡 witness：公开曾达到不安 3 的初始普通人可由病毒临时变为杀人狂；都市密谋达到 2 时，因子也可解释关键人物能力导致的死亡失败。相同 seed 下错误首选相对真实候选的权重差距约从 16:1 降到 4:1，最终仍为 6/9；这证明因果修正有效，但未公开的因子具体持有者仍需要主动调查才能区分。

这仍是单种子诊断；并行运行两组只用于规则覆盖，耗时不可比较。Oracle 候选不是穷举，也没有通用跨日搜索树；当前仅为已知的规则阈值加入可审计候选。下一步应固定少量失败轨迹做反事实验证，再把通过验证的候选和估值用于公平信息 AI。

## BTX 全剧本同预算覆盖

为了让高成本朴素版覆盖全部 11 个 BTX 剧本，另用 seed 0 和共同的 `4×4` 预算各跑一遍：

| 策略 | 胜局 | 胜率 | 平均实际决策 | 平均搜索节点/局 | 平均整局耗时 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 朴素 MCTS | 5/11 | 45.5% | 289.0 | 832.9 | 36.586 秒 |
| 优化 MCTS | 9/11 | 81.8% | 360.6 | 630.6 | 5.060 秒 |

命令示例：

```powershell
python -m benchmarks.ai_self_play --all-recorded --games 5 --nodes 8 --depth 8 --strategy optimized --protagonists baseline --progress
python -m benchmarks.ai_self_play --module BTX --games 1 --nodes 4 --depth 4 --strategy naive --protagonists baseline --progress
```

## 可配置跨日 rollout 首轮消融（2026-09-22）

两个已知剧本 Oracle 先加入 `day / loop / match` 稳定边界；后续黑方使用固定定式策略，后续红方继续使用其“已知剧本”的诊断前提。公平粒子集成版随后复用相同框架，但后续红方只能读取当时公开视图，不能针对每个粒子的真实身份选择行动；最终猜测也不能逐粒子开眼，因此公平版暂只开放 `day / loop`。长 horizon 与短 horizon 同时保留，后者用于长分支同分时的可靠破同分；没有加入固定信息能力奖励。

FS01 对固定黑方 seed 0–4、每次 12 个候选：

| 红方 | 日末档 | 轮回档 | 日末／轮回平均整局耗时 |
| --- | ---: | ---: | ---: |
| 明牌 Oracle | 4/5 | **5/5** | 2.3 / 11.0 秒 |
| 暗牌 Oracle | 5/5 | 5/5 | 3.5 / 35.9 秒 |

早期原型曾让后续红方退回公开单牌策略，并让后续黑方逐行动穷举一层估值；它既遗漏 Oracle 已知的未来事件当事人，又使一次整局超过数分钟。该版本未保留。当前实现按距离递减处理已知未来当事人，并用固定策略补全黑方；FS01 明牌 seed 3 从每轮重复败于第 3 天少女自杀修正为首轮存活。

公平粒子版使用固定黑方、12 候选，结果如下。胜局是主人公整局胜；“失败轮回”是每局平均值：

| 剧本与种子 | 日末档胜局 / 失败轮回 / 均时 | 轮回档胜局 / 失败轮回 / 均时 |
| --- | ---: | ---: |
| FS01 seed 0–2 | 2/3 / 1.67 / 2.39 秒 | 2/3 / 1.67 / 8.33 秒 |
| FS02 seed 0–2 | 2/3 / 2.67 / 8.44 秒 | 2/3 / 3.33 / 32.21 秒 |
| BTX01 seed 0–1 | 1/2 / 3.00 / 14.42 秒 | 1/2 / 3.50 / 47.96 秒 |

这组样本证明跨日模拟和信息边界可以运行，但**没有证明棋力提高**：公平版胜负完全相同，部分失败轮回增加，耗时约为 3–4 倍。主要风险是固定后续策略的误差随 horizon 累积，而不是规则结算本身。因此大厅默认仍使用 `day`；`loop` 仅用于后续策略模型、同墙钟预算及学习型估值标签的消融。复现时使用 `--protagonist-horizon day|loop`；旧参数名 `--oracle-horizon` 保留为别名。

## 公平版黑方策略维度（2026-09-22）

后续黑方不再为每个粒子随机锁定唯一一条路线。每个完整假想世界按其真实主线、身份、事件当事人和当前状态动态实例化策略 ID；同一红方三牌候选分别面对至多三条适用路线，再取该世界的最坏结果。黑方读取粒子内真相符合剧作家信息边界；红方根行动及后续公开策略仍不能读取该真相。实现不含官方剧本编号映射。

使用固定黑方、节点 8、深度 12 的首轮成对消融：

| 剧本／horizon | 种子 | 单路线黑方胜 | 三路线黑方胜 | 单路线／三路线均时 |
| --- | ---: | ---: | ---: | ---: |
| FS01 / day | 0–4 | 4/5 | 2/5 | 2.05 / 2.35 秒 |
| BTX01 / day | 0–1 | 0/2 | 0/2 | 7.03 / 3.91 秒 |
| FS01 / loop | 0–1 | 1/2 | 2/2 | 6.64 / 13.29 秒 |

表中仍记剧作家胜局。FS01 日末档的主人公由 1/5 提高到 3/5，是首个正向信号；BTX01 胜负未变，三路线局反而因整局决策数较少而更快，不能解释为单次搜索更便宜。FS01 轮回档由主人公 1/2 降为 0/2且耗时约翻倍，说明“每个世界一律取最坏定式”的误差会随跨日累积。这证明二维采样与信息隔离可运行，但还不能证明普遍棋力收益。后续应在相同墙钟而非相同候选数下比较，并将“黑方可选路线”扩展为少量联合搜索结果，同时消融概率均值、尾部风险和最坏值；`--mastermind-policy-samples 1|3` 可复现当前消融，trace 的 `policy_rollouts`、`mastermind_policy_samples` 和聚合方式用于审计。

## 日末信息潜力微奖励（2026-09-22）

公平粒子版新增基于公共粒子熵的未来信息潜力，检查最低轮回、好感阈值、本轮剩余日期／`g1/g2`、合法目标、已知事实与能力使用状态；奖励严格位于存活率和危险尾部之后。`--information-reward-weight 0` 可关闭评分而保留相同候选覆盖，默认 `0.01`，单候选 bonus 不超过 `0.012`。

全部 11 个 BTX 官方剧本、seed 0、节点 4／深度 6、固定黑方的成对结果完全相同：两组主人公均为 **6/11**，信息能力均请求并成功 **10** 次，无奖励／默认奖励平均整局分别为 11.16／10.99 秒；逐局最终猜测也相同。FS01 和 FS02 的 seed 0–4 分别保持主人公 3/5 与 4/5。`0.02` 在三个代表剧本上也没有产生额外变化，故不再抬权重。

结论不是负面失败：trace 证明奖励确实在部分日计划参与破同分，且没有增加好感牌滥用或降低固定样本生存；但当前样本**不能证明棋力提升**。详细公式、角色例子和审计项见 [信息价值设计](information-value-design.md)。

## BTX 轮回难度配对基线（2026-09-22）

卡面多轮回数已拆为独立剧本 ID：最少轮回为标准版，随后是 Easy / Very Easy。旧结果使用源 JSON 的最大默认
轮回数，因此双档卡实际都是 Easy，三级卡实际是 Very Easy。新的 `ai_difficulty_matrix` 会用相同种子和预算逐版本
串行运行，并把 `loops`、`difficulty`、失败轮回、最终猜测与耗时写入结果。

工程基线覆盖 BTX 的 9 张多难度卡（8 张双档、1 张三级），每个版本 seed 0–4。固定策略黑方；公平粒子红方
节点 4、深度 6、日末 horizon、每世界至多三条黑方路线，信息权重 0.01：

| 难度 | 对局 | 主人公胜（Wilson 95% CI） | 平均失败轮回 | 失败／可用轮回 | 进入最终猜测时的合计正确率 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 标准 | 45 | 13/45，28.9%（17.7%–43.4%） | 2.76 | 80.0% | 107/280（38.2%） |
| Easy | 45 | 14/45，31.1%（19.5%–45.7%） | 3.31 | 74.5% | 97/282（34.4%） |
| Very Easy | 5 | 2/5，40.0%（11.8%–76.9%） | 3.60 | 72.0% | 9/27（33.3%） |

标准版与 Easy 的 45 组同种子配对中，Easy 独有胜局 5 组、标准版独有胜局 4 组、两边都赢 9 组、两边都输 27 组。
Easy 的净胜局只增加一局，置信区间高度重叠；失败轮回的绝对数因可用轮回更多而上升，但占可用轮回的比例下降。
当前不能声称总体胜率显著提升。

额外轮回确有正向样本，例如 BTX02 seed 0 从标准版黑胜翻为 Easy / Very Easy 红胜，BTX04 seed 2 也仅 Easy 获胜；
但反向样本同样存在。BTX01 seed 2、BTX02 seed 2/4、BTX09 seed 3 都是标准版红胜、Easy 黑胜；BTX06 seed 4
更从标准版最终 3/9 降至 Easy 0/9。规则上额外轮回不会降低最优策略价值，因此这些反向结果属于当前有限候选、粒子重采样、
后验校准或最终猜测策略的缺陷，不是剧本难度反转。上述轨迹应优先做定点 trace 审计，暂不继续提高信息奖励。

BTX06 seed 4 的首轮定点审计已排除“硬 witness 错删真实世界”：Easy 终局中真实配置对全部硬 witness 仍兼容，
`belief_failure` 也始终为 `None`。真正的问题是最终联合后验仍复用了容量 192 的随机合法配置库；真实完整配置不在这
192 个候选中，因而没有可报告的真实配置排名，联合 MAP 只能在稀疏库里选择，最终 0/9。主规则 `bomb` 已正确进入所有
高位候选，错误主要发生在规则 X 与具体身份分配。后续基准现单独记录真实配置是否仍硬兼容、是否进入终局候选、候选数及
进入时的排名；这些字段只存在于知道答案的离线基准，不进入玩家视图、AI 输入或 replay。

因此下一修复不是扩大行动粒子，也不是抬高信息奖励，而是把终局猜测器从行动规划粒子中分离：基于全部合法身份槽求
受约束的整套 MAP（或可校准近似），并保留完整身份组合合法性。完成后先复测 BTX06 seed 4，再检查其余反向样本。

### BTX06 轮回末 witness 与公开地点防守回归（2026-09-22）

定点 trace 还发现，终局猜测之前已经存在更早的行动缺陷：前一轮明明以学校 2 以上密谋结束，下一轮的少量粒子和候选
仍可能没有把“炸弹 + 学校初始魔女”转成稳定防守。现在轮回末同时记录炸弹主规则软压力与“高密谋区域初始角色中有魔女”
的局部联合软证据；已知炸弹世界会显式提出对魔女初始区域使用禁止密谋的三牌。

另外增加一个独立于身份粒子的公开先验：只要剧作家当天在区域放牌，就为该区域保留禁止密谋候选，并给予 0.08 的有界
地点防守奖励。排序仍先比较 horizon 存活率，奖励不能挽救在任一存活维度落后的计划；它只在同等存活候选之间影响尾部值
和日末值。它不读取暗牌牌面，也不把区域牌硬判为密谋。

复用难度矩阵的固定黑方、双方 `4×6`、seed 4 定点回归：

| 版本 | 轮回数 | 修改前异常结果 | 修改后结果 | 修改后失败轮回 |
| --- | ---: | --- | --- | ---: |
| 标准 | 3 | 黑方胜，最终猜测 4/9（仅 witness 修复但无地点奖励时） | 主人公胜 | 1 |
| Easy | 4 | 主人公胜，但需失败 2 轮 | 主人公胜 | 1 |

修改后两版都在首轮观察到规则 Y 失败，次轮即持续防守学校并存活获胜；相关 witness、Oracle 与粒子集成测试 48 项通过。
这是单一已知失败种子的正确性回归，不是胜率估计。仍需在其他 BTX 剧本消融地点奖励；本局没有进入最终猜测，因此终猜
候选空间的修复由下一节的独立回归验证，而不是由本局胜负推出。

### 独立终局合法配置 MAP（2026-09-22）

最终猜测已从行动粒子 reservoir 分离。新求解器枚举所有合法规则组合，用多重身份槽和公开硬证据计算完整合法空间；对软
witness 只枚举能改变其满足状态的小型身份约束，再把其它对称身份槽合法补全。因此最终选择仍是一整套身份 MAP，不是逐角色
最大边际拼接，也不需要实际生成数百万套只有普通人位置不同的配置。事件当事人和当天暗牌继续留在各自维度，不污染最终身份
组合。

构造回归中，BTX06 的求解结果在行动身份 reservoir 容量为 1 和 512 时完全一致，且合法候选数大于 192。BTX01 seed 0、固定
黑方、双方 `4×6` 的实际终局中，求解器报告 **84,504** 套硬兼容完整配置，真实配置硬兼容并明确属于精确空间；终猜仍只有
**1/8**。这不是求解退化的证据，而是把问题准确地从“随机 192 套没有真值”移到了“公开轨迹对完整身份仍缺乏区分力／当前
先验与软 witness 排序不足”。前 8 个 `belief_setups` 只用于展示最高分代表，不能再用真值是否出现在这 8 项中判断粒子耗尽。

同预算 BTX01 难度配对的单 seed 结果为标准版黑方胜、4 轮均失败并终猜 1/8；Easy 在首轮直接存活获胜而未进入终猜。样本仅
用于接口与候选空间正确性，不能用于比较难度或棋力。下一步先复跑此前标记的异常 seed，报告精确空间规模、硬兼容性和猜对
人数，再决定补哪一类 witness；不因 BTX01 单局低正确数直接抬高某个身份权重。

精确求解器完成后复查旧反向样本：BTX02 seed 2–4 中标准 / Easy / Very Easy 分别为 **1/3、2/3、2/3**，不再出现
标准胜而 Easy 负；BTX09 seed 3 两版均负，终猜从标准 5/10 改善到 Easy 6/10；所有进入终猜的真实配置均通过硬约束并在
精确空间内。BTX02 Very Easy seed 4 仍出现 Easy 胜、Very Easy 4/9 负，说明行动候选或随机流仍可能非单调，不能把精确终猜
误称为已解决全部难度反向问题。

BTX01 seed 2 的四次失败均为最后一天时间旅行者可选能力宣告失败。该结算直接记录 `protagonists_lost(day_end)` 后进入
`loop_lost`，不会先产生正常的 `day_ended`；旧 witness 只识别后一种时序，因此完全漏掉时间旅行者压力。修复后编译器同时
接受这两种公开时序，AI 正确猜出 AI 角色的时间旅行者身份，标准版终猜由 **1/8 提升到 2/8**；Easy 版以多一个轮回存活获胜。
标准版仍有 **421,349** 套硬相容完整配置，真实配置在其中，剩余低正确数应由主动调查或新增可证明现象改善，而非继续抬高
时间旅行者权重。此 witness 只读取公开的失败时间点、最终日和各角色公开好感，不读取内部失败诊断。

随后定位到难度配对的另一个噪声源：粒子 RNG 曾哈希完整玩家视图，里面包含剧本 ID、标题、总轮回数和事件翻译文本。
因此标准 / Easy 即使处于相同结构化局面，也会抽到不同隐藏世界。采样哈希现忽略这些难度／展示字段，但估值仍能读取总轮回数；
这相当于给配对实验使用共同随机数，不会阻止 AI 利用“还有几轮”制定策略。BTX02 seed 2–4 复测后，标准 / Easy / Very Easy
主人公胜分别为 **1/3、1/3、2/3**，每个 seed 的结果均随额外轮回单调；此前 Easy 胜而 Very Easy 4/9 负的 seed 4 变为三档
都在首轮失败、次轮获胜。seed 2 则标准和 Easy 均负、Very Easy 利用第五轮获胜。该改动会改变旧基准的具体随机样本，完整
多剧本矩阵需在下一次策略验收时重新建立，旧总体胜率只保留为历史记录。

### 共同随机数与时间旅行者硬 witness 回归（2026-09-23）

难度配对继续审计后发现，`loop_lost` 事件中的 `remaining` 仍把总轮回数间接带回采样哈希。去除这个难度派生字段后，
相同公开历史的标准 / Easy 会继续使用共同随机样本；总轮回数仍留在正式视图中供长期策略读取。尚未加入时间旅行者专项筛查时，
全矩阵标准 / Easy / Very Easy 主人公胜为 **23/45、25/45、3/5**，45 组标准→Easy 配对中 Easy 独胜 2、标准独胜 0。

随后按 [系统 FAQ](https://w.atwiki.jp/rooper/pages/21.html) 核对：时间旅行者在最终日日末、友好 **3 未满**时可使主人公失败；
该角色还强制忽略自身的禁止友好。实现据此区分三类公开证据：

- 正常 `day_ended` 后才看到轮回失败仍有多种解释，只保留软压力；
- 明确的 `protagonists_lost(day_end)` 是时间旅行者专用结算，生成硬身份候选，友好达到 3 的角色被硬排除；
- 同一角色上“禁止友好”与好感牌相撞而友好仍成功增加，直接公开证明该角色是时间旅行者。

行动候选同时保留一组三牌联合筛查：已有硬时间旅行者压力后，用不同主人公的好感牌把不同候选推到友好 3。计划仍与所有粒子
交叉模拟；额外奖励每筛查一人 0.004、总计最多 0.012，并严格排在 horizon 生存、当日生存、尾部风险和日末估值之后。

BTX03 是稳定回归目标。修改前标准 / Easy 均 **0/5**；只启用硬 witness、关闭筛查时均为 **2/5**；完整启用后为
**3/5、5/5**。另外两个会出现时间旅行者压力的多难度剧本也改善：BTX01 从 3/5、3/5 到 **4/5、5/5**，BTX09 从
1/5、1/5 到 **2/5、3/5**。结合其它不受此候选影响的剧本，最终矩阵为：

| 难度 | 主人公胜 | 进入终猜时正确率 | 配对说明 |
| --- | ---: | ---: | --- |
| 标准 | 28/45（62.2%） | 74/155（47.7%） | — |
| Easy | 34/45（75.6%） | 52/101（51.5%） | Easy 独胜 7 |
| Very Easy | 3/5（60.0%） | 8/18（44.4%） | 仅 BTX02 |

标准→Easy 的 45 组配对为：Easy 独胜 7、标准独胜 1、都胜 27、都负 10。唯一反向是 BTX09 seed 3；两档该剧本总体都比
修改前提高 1–2 局，因此保留异常供后续跨日策略审计，不针对单 seed 撤销硬证据或削弱候选覆盖。所有进入终猜的真实配置仍
通过硬 witness；并行运行的耗时受 CPU 竞争影响，本节不据此比较性能。

复现命令：

```powershell
python -m benchmarks.ai_difficulty_matrix --module BTX --games 5 --seed 0 --nodes 4 --depth 6 --protagonist-nodes 4 --protagonist-depth 6 --mastermind fixed --protagonists particle_ensemble
```

后续先保存逐局 JSON 并审计上述反向／猜测异常轨迹；分钟档只选通过正确性检查的代表轨迹。不能把节点预算
称作严格同墙钟，加入 `--time-limit-ms` 与 `--protagonist-time-limit-ms` 后才是每次决策墙钟实验。

BTX09 seed 3 的后续审计确认，两档难度在第 3 轮第 3 日以前使用相同公开状态、世界样本和候选；分歧来自截断估值，
而非共同随机数失效。日末 horizon 原先还把“已经花费的轮回数”放进 `tanh`，该项对当前所有三牌候选都是常数，却会
改变候选间的数值距离；现已在日末比较中移除，轮回末／整局 horizon 仍保留真实比赛进度。5-seed 回归结果保持标准
**2/5**、Easy **3/5**，配对仍为 Easy 独胜 2、标准独胜 1、都胜 1、都负 1。剩余反向并非纯估值常数：BTX09 的
“因果线索”会根据未来是否还有轮回改变规则效果，因此不同总轮回数确实可能产生不同的合理日末价值。后续用真实跨日
rollout 审计这个选择，不为单个 seed 再加固定奖励。

## 解释限制

### 好感接受硬 witness 消融（2026-09-23）

新增 `public_refusable_goodwill_accepted`：公开接受一项本可拒绝的好感能力时，发动者不可能是 FS/BTX 的强制拒绝者（邪教徒、
魔女）。不可拒绝能力明确排除，缺少 `unrefusable` 元数据的旧回放保持未知。通用参数
`--disable-witness-source public_refusable_goodwill_accepted` 可单独关闭该来源。

构造的 6 角色 BTX 信息集上，关闭 witness 时完整合法身份空间为 **28,240**，开启后为 **25,025**，恰好排除 3,215 个把
发动者分配为邪教徒或魔女的配置，真实相容配置不受影响。最初一次全局对照漏把 `role_not_in` 接入精确身份后验，因而得到
错误的“无变化”结论；补齐接线后重新运行全 BTX 多难度、seed 0、固定黑方、4 节点消融：关闭时标准 / Easy / Very Easy
为 **6/9、6/9、1/1**，开启时为 **7/9、8/9、1/1**。翻转来自 BTX03、BTX04 与 BTX09 Easy；BTX02 反向由红胜变黑胜。
这仍是单种子工程回归，必须扩大配对种子后才能判断净棋力。

### 正常轮回末失败的完整解释约束（2026-09-23）

当 `day_ended` 后直接出现 `loop_lost`，且中间没有亲友身份公开时，失败必由当前主规则的轮回末条件解释。新增硬 witness
`public_normal_loop_end_loss` 同时保留所有合法解释：FS 的守护此地/复仇火种，以及 BTX 的封印/契约/改变未来/定时炸弹，
其中身份与初始区域的关联在同一个联合约束内判断。特殊时间旅行者失败、关键人物立即死亡和亲友死亡不会误入此路径。

构造的“神社密谋 2 后正常失败”信息集从 **28,240** 套缩到 **8,315** 套，只剩封印 7,560 套，以及魔女初始区域确在神社
的炸弹 755 套。全 BTX 多难度 seed 0 中，关闭与开启该来源的胜负相同，均为 **7/9、8/9、1/1**；BTX06 的后续联合出牌
发生变化，说明约束已进入行动后验，但这一个种子没有额外胜局。所有已录入 BTX 真实轨迹继续通过硬兼容检查。

### 禁止密谋被忽略的邪教徒 witness（2026-09-23）

若公开牌中全局恰好一张禁止密谋，它与密谋牌作用于同一目标，移动结算后密谋仍然增加，则 FS/BTX 规则下必有邪教徒对该
区域发动忽略效果。新增 `public_intrigue_forbid_ignored` 不把目标误认成邪教徒，而是根据当时公开位置建立“合法发动者中至少
一人是邪教徒”的联合硬约束；两张以上禁止密谋因全局互相抵消，不产生该 witness。复活、离场和替补登场也纳入位置重建。

构造的 6 角色信息集里，三个同区域候选至少一人为邪教徒，使完整身份空间从 **28,240** 降至 **7,380**。全 BTX 多难度
seed 0 未实际触发该现象；开启/关闭均为标准 **7/9**、Easy **8/9**、Very Easy **1/1**，逐局结果不变。因此本轮只证明
约束的候选收缩和零回归，实际棋力贡献要由包含该对抗的后续种子验证。

### 日末主人公死亡的身份路线 witness（2026-09-23）

FS/BTX 的 `heroes_died(day_end)` 只有两类身份能力可以解释：自身密谋至少 4 的杀手，或不安至少 3 且密谋至少 1 的求爱者。
新增 `public_day_end_hero_death` 将两类公开阈值候选保存在同一个硬联合约束中；事件阶段的医院事故和被保护阻止的死亡不会
产生该 witness。构造信息集从 **28,240** 套缩到 **2,932** 套。全 BTX 多难度 seed 0 没有实际触发该专用死亡，启用/关闭
仍同为标准 **7/9**、Easy **8/9**、Very Easy **1/1**；后续需用能稳定走到杀手/求爱者路线的黑方种子验证实战贡献。

### BTX 死亡后立即失败的关键能力 witness（2026-09-23）

BTX 中，一批角色死亡后若没有经过正常 `day_ended`、主人公死亡或其它失败事件便立即结束轮回，则死亡批次中必有角色持有
关键人物能力。新增 `public_btx_immediate_death_loss` 保留全部同时死亡者；都市密谋不足 2 时要求其中有人初始身份为关键人物，
都市密谋达到 2 时同时允许其中有人是不安定因子。普通亲友死亡、医院事故杀死主人公和正常主规则失败不会进入该路径。

构造的单一死者、都市密谋不足 2 的信息集从 **28,240** 套缩到 **1,955** 套，所有剩余配置都把死者分配为关键人物。与加入
前完全相同的全 BTX 多难度 seed 0 对照中，主人公由标准 / Easy / Very Easy **7/9、8/9、1/1** 提升到
**8/9、9/9、1/1**；BTX02 的标准和 Easy 从终猜 5/9 的黑方胜翻为首轮失败、次轮存活的红方胜。BTX08 多损失一轮但仍获胜；
其它逐局胜负不变。单种子 Easy 已到 100%，下一步扩大配对种子；若优势保持，再按计划增强黑方而不是继续只看 seed 0。

### rollout 共同随机数修复后的 5-seed 基线（2026-09-23）

外层粒子采样虽已剔除剧本 ID、标题和总轮回数，但 Oracle rollout 内的固定策略种子仍直接使用完整 `state_key()`；因此相同
隐藏世界与公开局面的 Standard/Easy 会随机选择不同目标。现在 rollout 使用包含真实棋盘和黑方秘密、但剔除难度与显示元数据
的结构化私有状态键。总轮回数仍可进入显式长 horizon 估值，只是不再暗中重洗随机策略细节。

BTX02 seed 4 从 Standard 负、Easy 胜、Very Easy 负变为三档均负；BTX08 seed 2 与 BTX09 seed 4 从 Standard 胜、Easy 负
变为两档均胜。完整 5-seed、固定黑方、4 节点矩阵为：

| 难度 | 主人公胜 | 进入终猜时正确率 | 失败/可用轮回 |
| --- | ---: | ---: | ---: |
| Standard | 31/45（68.9%） | 47.2% | 53.5% |
| Easy | 34/45（75.6%） | 48.0% | 47.5% |
| Very Easy | 2/5（40.0%） | 51.9% | 64.0% |

Standard→Easy 配对为 Easy 独胜 3、Standard 独胜 0、都胜 31、都负 11；25 个进入终猜的真实配置全部通过硬约束。Very Easy
仅对应 BTX02，同一 5 个 seed 的逐局胜负现在与 Standard/Easy 完全一致，40% 不能与全剧本总体直接比较。红方总体优势尚未
达到需要立即转向强化黑方的程度；下一轮继续 witness 覆盖，同时用更强黑方复测，避免只对固定策略过拟合。

### 自杀死者当事人 witness 消融（2026-09-23）

成功发生“自杀”且强制效果确实造成角色死亡时，公开规则可确定死者就是该日事件当事人；死亡被时间旅行者或护卫防止时不作
反向推断。构造信息集的当事人维度从全体角色收缩为唯一死者，并覆盖了打工仔替补映射。全 BTX 多难度 seed 0、固定黑方、
4 节点对照中，启用与 `--disable-witness-source public_suicide_victim` 的逐局结果完全相同：标准 **8/9**、Easy
**9/9**、Very Easy **1/1**，唯一终猜均为 7/10，真实配置硬兼容 1/1。本批轨迹没有形成能改变后续决策的成功自杀死亡，
因此结论是约束正确且无退化，不声称已有胜率收益。

### 简单事件效果 witness 批量消融（2026-09-23）

一次加入两类可独立关闭的硬来源：失踪实际移动者精确指认当事人，以及谋杀死者／蝴蝶效应
目标把当事人限制在事件发生时的同一区域。为支持最后一类，新的 `incident_status` 日志会冻结当时公开位置；旧日志缺少该字段
时不生成约束。构造测试分别验证了无效果、移动被阻止、打工仔替补、谋杀排除死者本人和 source 消融。

全 BTX 多难度 seed 0、固定黑方、4 节点下，同时关闭两个新 source 与全部启用的 19 局逐局相同：标准 **7/9**、Easy
**8/9**、Very Easy **1/1**；3 个进入终猜的真实配置全部硬兼容。保存历史位置本身是新增公开证据，会改变信念随机键，因此这组
绝对数字不与旧日志格式的 seed-0 基线直接比较；有效消融比较只使用同一新格式下的启用/关闭两组。本批没有观察到胜负收益，
但构造当事人维度能从全角色集合收缩到精确角色或同区域候选集合。

### 时间旅行者免死 witness 消融（2026-09-23）

BTX 中 `death_prevented` 只由时间旅行者的“不会死亡”产生；护卫消耗与侍从替死分别记录为 `guard_spent` 和
`death_replaced`，不生成这条身份结论。不安定因子也不会获得时间旅行者能力。因此新增 BTX 专属硬组件
`btx.time_traveler_death_prevention`，source 为 `public_time_traveler_death_prevention`。

全 BTX 多难度 seed 0、固定黑方、4 节点消融中，启用后标准版从 **7/9** 提升为 **8/9**，Easy 保持 **8/9**，Very Easy
保持 **1/1**。BTX03 标准版从耗尽 4 轮并终猜 3/9，变为零轮回失败直接获胜；Easy 虽然两组都胜，也从损失 4 轮降为 0 轮。
标准版平均失败轮回从 1.89 降至 1.44，Easy 从 2.00 降至 1.56。所有进入终猜的真实配置仍硬兼容，没有反向胜负样本。

同批补充了谋杀位置 witness 的侍从替死反例：发生 `death_replaced` 时使用公开的原始 `protected` 目标，而非最终死者；护卫或
时间旅行者阻止谋杀死亡时，公开的被保护目标仍能安全提供同区域约束。

### 病毒阈值与公开身份 witness 消融（2026-09-23）

新增 BTX 专属组件：公开查出杀人狂，但本轮完整记录里该角色从未达到不安 3，则排除“初始普通人＋病毒”的解释；公开查出普通人，
且本轮存活时曾达到不安 3，则排除病毒副规则。计数物变化的中间步骤也计入触发，轮回开始时重置；旧日志缺少轮回开始记录时不下硬结论。
约束同时接入行动粒子后验和最终猜测的精确身份求解。

全 BTX 多难度 seed 0、固定黑方、4 节点下，启用与同时关闭两个 source 的 19 局逐局一致：标准 **8/9**、Easy **8/9**、
Very Easy **1/1**；两组唯一终猜都是 7/10，真实配置保持硬兼容。这批实际轨迹没有让新约束改变行动或猜测，因此只确认了构造
情形的候选空间收缩和无退化，不归因任何胜率收益。

同批优化前先比较了 BTX09 的日末与轮回末 horizon。日末为标准 **2/5**、Easy **3/5**；轮回末为标准 **2/5**、Easy
**4/5**，但平均整局耗时从约 6–8 秒升至 13–17 秒，并出现 seed 4 标准胜而 Easy 负的新反向样本。跨日信息有价值，当前
固定后续策略和选择聚合仍不够稳定，暂不直接改默认值。

### 事件 P0 与因果线正向 witness 消融（2026-09-23）

本批新增四个独立 source：`public_suicide_prevented_target` 将自杀被护卫、不死或侍从替死保护的**原目标**锁定为当事人；
`public_guru_incident_doubled` 从预定事件效果加倍确定教主当事人；`public_effective_incident_excludes_black_cat` 从
`incident_ended.effective=True` 排除黑猫当事人；BTX 专属 `public_threads_loop_start_paranoia` 从完整上轮好感记录及
新轮起始的 +2 不安确定因果线。仅编译正向、实际公开发生的效果；不把未发动或没有效果写成硬反证。每个 source 都有
单独关闭、合法反例和真实引擎日志测试。

固定黑方、粒子集成红方、双方各 4 节点／深度 6，开启与关闭本批全部 source 的同种子对照：FS01/02 的种子 0–2、
标准和 Easy 合计 12 局，**两组均 12/12 主人公胜**，逐局失败轮回也相同；BTX 全部 19 个难度变体、种子 0，
**两组均 17/19 主人公胜**，标准 8/9、Easy 8/9、Very Easy 1/1。已录入剧本没有教主／黑猫，不能用这批整局结果
评价它们；构造信息集的逐 source 消融确认各自都能缩小事件当事人候选且保留真值。

BTX09 是本批因果线的实际压力测试。标准／Easy 各取种子 0–4，**开启**时主人公分别 **1/5、2/5**；仅关闭因果线
source 时分别 **3/5、3/5**。开启组标准失败轮回平均 3.4，关闭组 2.8；Easy 为 4.0 对 3.2。种子 0 的标准版两组
都进入终猜：完整合法身份候选从关闭时 **1,617,712** 缩至开启时 **20,584**，真值仍符合全部硬 witness，但猜对数
从 **7/10 降至 6/10**。多种子终猜游戏数量不同，不宜直接比较其平均正确率或平均候选数。上述反向结果不能证明
因果线推理错误，也不能证明一般棋力必然下降；它提示**后验集中后，固定容量行动粒子和短日末规划可能选择更差的牌**，
应先审计 BTX09 seed 1/3 的候选覆盖、世界权重、行动评分与黑方实际路线，再考虑调权或扩大预算。不要为提高这批胜率
删除真实成立的硬线索。

复现入口：`python -m benchmarks.ai_difficulty_matrix --module BTX --games 1 --seed 0 --nodes 4 --depth 6 --protagonist-nodes 4 --protagonist-depth 6 --mastermind fixed --protagonists particle_ensemble`；
逐 source 通过重复 `--disable-witness-source SOURCE` 关闭。BTX09 定点组再加
`--scenario-base official-btx-09-those-with-antibodies --games 5`。这是**节点预算、固定对手**的小样本工程评估，不是等墙钟
或强黑方棋力结论；本批之后需在更强黑方、更多种子和等时间预算下重测。

### BTX09 因果线与时间旅行者行动修复（2026-09-23）

反向局审计发现：硬 witness 正确收缩了副规则，却改变了仅 4 个行动粒子的身份构成；更直接的问题是时间旅行者的公开候选
防守原本只奖励**当天达到友好 3**，且只生成好感排序最前一组候选的三牌。若真实角色在队列后半段，0→2 的跨日准备既
得不到奖励，也根本不会进入候选池。BTX09 seed 1 的真实时间旅行者记者连续四轮未获得好感，就是这一覆盖缺口的实例。

修复后，在已经有时间旅行者失败压力且公开候选**超过六人**时保留前、后两组筛查候选；0→2 等仍可在本轮凑到 3 的进度
获得有界奖励，最后一天无法完成的半途投资不计分。因果线风险只在规则已确认、当前不是最后轮回时，对从 0 新增好感的
角色计入；它按距离下次轮回的天数折扣，至多 0.008，尸体仍计入。已有好感再增加不重复处罚，信息类能力的独立奖励
继续保留。风险与信息收益都只用于当日／日末存活之后的细分排序，不覆盖存活判断。额外筛查组由**候选规模**而非
“因果线已确认”触发：BTX09 有些轮回尚无足够公开记录确认因果线，过早关闭会重新退化；BTX01 只有五名候选，
无条件扩组曾使标准版从赢变输。按规模收窄后两局定点均恢复。

相同 4 节点／深度 6、固定黑方、BTX09 种子 0–4 配对结果：

| 配置 | 标准主人公胜 | Easy 主人公胜 |
| --- | ---: | ---: |
| 修复前，因果线 witness 开 | 1/5 | 2/5 |
| 修复后，因果线 witness 开 | **3/5** | **4/5** |
| 修复后，仅关闭因果线 witness | 3/5 | 3/5 |
| 修复后，仅关闭跨轮好感风险项 | 3/5 | 4/5 |
| 修复后，信息收益权重置零 | 3/5 | 4/5 |

单独把行动粒子数从 4 加到 12、仍保留 4 个三牌候选的早期对照为 1/5、1/5，说明问题不只在粒子数量；
`--protagonist-particles` 现允许独立控制这个维度。更早一次把信息收益和风险直接加进最坏世界的原始分数，Easy 降到
1/5，故已撤销该激进排序。最终方案的提升主要来自**候选覆盖和可完成的跨日进度**；最终触发条件下风险和信息微量项
各自关闭都未改变这十局胜负，所以暂不宣称它们有棋力收益，也不据此继续抬权重。

FS01/02 种子 0–2 的标准／Easy 共 12 局仍为主人公 12/12，逐局结果不变。全 BTX 多难度种子 0 的 19 局
从修复前主人公 **17/19** 变为 **19/19**（标准 9/9、Easy 9/9、Very Easy 1/1）。该固定黑方、单种子矩阵
已接近饱和；仍需更多种子与强黑方／等墙钟复测，不能把小样本胜率视为稳定棋力。

- 这是低预算、低样本量的工程回归，不是棋力排名；单种子 BTX 表格尤其不能视为真实胜率。
- 基础主人公 AI 比纯随机更有一致性，但没有身份信念、协同规划或搜索，远弱于熟练真人。
- MCTS rollout 中的主人公动作仍是随机策略，并未模拟这个外部基础对手，因此存在对手模型偏差。
- 整局耗时同时受搜索实现、对局长度和强制行动数量影响。比较搜索效率时还要结合实际决策数和累计节点数。
- 优化版使用新的 FS/BTX 场景化估值，朴素版保留旧通用估值；差异同时来自搜索实现和估值函数，后续需做估值消融。

### BTX 恋爱死亡 witness 与联合黑方压力测试（2026-09-23）

新增 BTX 专属 `public_love_death_reaction`：连续死亡日志紧接某存活角色不安 +6 时，确认恋爱风景线和收取者的恋爱身份；仅当同批死亡只有一人时，也约束死者属于配对身份。真实 BTX01 日志、多人同批死亡、旧日志缺字段、非 +6 反例、逐 source 关闭均有测试。BTX01 构造局中，仅关闭该 source 时精确身份候选为 **421,349**，启用后 **6,996**；真实剧本仍匹配。固定黑方、粒子集成红方、双方 4 节点／深度 6 的 BTX01 标准和 Easy 各 seed 0–2，启用与关闭的结果均是红方 **3/3、3/3**，逐局失败轮回亦相同。这些自然轨迹没有提供可见胜率收益，不把定点候选缩减误称为棋力提升。

BTX 的黑方三牌联合搜索现作为可选 `joint_mastermind` 对手开放，不替换逐步 `strategic_mcts_mastermind`。BTX09 标准／Easy 各 seed 0–2、双方 4 节点／深度 6、相同红方粒子策略的对照：逐步黑方令红方胜 **0/3、1/3**，平均整局 **16.9、21.6 秒**；联合黑方令红方胜 **0/3、0/3**，平均 **24.8、37.2 秒**。因此同节点下联合搜索提高了这组样本的黑方结果，但时间成本很高。单次黑方决策限制 1 秒、上限 16 节点的 BTX09 seed 0 对照中，两者标准版都赢；Easy 则逐步黑方赢、联合黑方输（红方最终猜对 10/10）。这直接说明目前**没有稳定的等时间棋力优势**。当前联合黑方仍使用单日主人公回复模型，未显式计价身份泄露；后续应先优化跨日路线和信息暴露估值，再用更多种子等墙钟复测。

### 剧作家能力阶段来源析取 witness（2026-09-23）

新增 FS/BTX 公共组件 `common.mastermind_counter_sources`，分别用 `public_mastermind_intrigue_source` 和 `public_mastermind_paranoia_source` 消融。公开计数变化只说明至少一条合法来源路线存在：主谋可放角色／版图密谋，流言四起可放版图密谋；传谣人可给同区域角色不安，BTX 因子可复制传谣能力，满足条件的医生也可使用调整能力。使用公开移动记录还原当时位置，大人物领地和医生／因子未公开的前提保守放宽；御神木登场时不推断，因为同一时间点还有强制转移。最终猜测求解器为这种硬析取显式生成身份候选，避免默认填充身份时漏掉可行世界。构造反例、真实引擎三条来源、BTX 因子复制和逐 source 关闭均有测试。

BTX09 固定黑方、粒子红方、双方 4 节点／深度 6、标准／Easy 各 seed 0–2：启用与同时关闭两个 source 完全同轨，红方分别 **2/3、3/3**；唯一终猜均 **7/10**，真值硬兼容。此固定黑方样本未体现棋力差异。更强的策略黑方、seed 0 下，启用时红方标准 **0/1**（终猜 7/10）、Easy **0/1**（终猜 7/10）；关闭时标准 **0/1**（终猜 5/10）、Easy **1/1**。启用局的公开视图最多出现 14 条新不安来源证据、没有新密谋来源证据；对标准版逐次编译检查时，真实剧本均符合硬约束。标准版终猜改善与 Easy 胜率反向同时发生，说明在当前仅 4 个行动粒子、短日末规划下，后验改善仍可能改变行动采样并导致更差决策；不能把单种子差异当作稳定棋力结论，也不应删掉正确的硬证据来调胜率。下一步应定点审计 Easy 的决策候选覆盖与最坏世界评分，再决定是否增预算或修改聚合。
