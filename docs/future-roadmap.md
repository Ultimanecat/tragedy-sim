# 后续开发路线图

本文记录角色补齐之后的中长期工作。现有 Web、局域网房间、SSE、随机 AI、定式剧作家 AI、朴素与优化 MCTS
继续作为可运行基线；新功能仍通过 Python 后端的结构化行动、状态转移和信息视图接入。

## 实施顺序

```text
阶段 A：补齐正式角色（35 名，已完成）
        ↓
阶段 B：录入官方剧本（首批 22 个已完成，继续等待资料）
        ↓
阶段 C：策略搜索与信念 AI
        ↓
阶段 D：云端部署与远程联机（需要 ECS 实例）
```

阶段 B 依赖人工资料，可以在等待资料时先开展阶段 C；阶段 D 在确定云主机、域名和部署预算后进行。
每个阶段都不得在前端复制规则，AI 和网络客户端只消费后端提供的公开视图与 `ActionOffer`。

## 阶段 A：补齐正式角色（35 名，已完成，2026-09-15）

在现有 19 名角色基础上补齐通用正式角色：

- [x] 神格（本地中文卡名为“神灵”）
- [x] 大人物
- [x] 学者
- [x] 幻想
- [x] A.I.
- [x] 黑猫
- [x] 转校生

每名角色需要同时完成：

1. 卡面基础数据、好感能力、固有特性和剧本编写约束；
2. 所有适用规则集中的结算与信息边界；
3. JSON catalog、中英日术语、React 展示和 WebP 素材映射；
4. 正常发动、空发动、限次、拒绝、死亡和跨轮回等边界测试；
5. 全量 Python、Web 测试及生产构建。

推广角色和脚本集附加角色暂不属于本阶段；在上述七名稳定后另行排期。

随后已按官方角色总表补齐小女孩、教主、模仿者、御神木、妹妹、临时工/临时工？、从者和上位存在。
按项目范围排除 UP 主与仙人；临时工？作为临时工死亡后的替换面实现，不计额外初始登场位。当前 catalog 共 35 张角色卡。

## 阶段 B：录入官方剧本（进行中）

由用户提供可核对的官方剧本图片或数据。每个剧本人工录入 `scenarios/`，至少双重核对：

- 规则集、规则 X/Y、天数和轮回数；
- 登场角色与身份，包括局外人、A.I. 等特殊配役约束；
- 事件日期、事件类型、当事人与公开伪装信息；
- 公开特殊规则和剧本专属设置。

录入后使用严格校验器加载，并为每批剧本建立目录测试；不从来源不明的网络转录直接导入。

首批已根据用户放入 `references/` 的图片录入并验证 22 个剧本：FS 2 个、BT/BTX 11 个、MZ 4 个、MC 5 个。
大厅目录自动发现这些剧本并显示卡面允许的轮回数范围；当前以 JSON 的 `loops` 值作为默认轮回数。剩余官方剧本仍需
继续由人工提供和校对；集中完整试玩尚未完成。

## 阶段 C：更强的 AI

按可验证的能力逐层升级，始终保留随机 AI 和定式 AI 作为基线：

### C1：搜索基础设施与评估基准（已完成，2026-09-15）

- [x] 可复现随机种子、思考时间和节点预算；
- [x] 状态克隆/转移性能基准、终局奖励和有界启发式局面评价；
- [x] AI 决策追踪只进入进程内调试记录，不向对手或 replay 泄露；
- [x] 固定剧本自对弈胜率/耗时基准和真实规则终局回归测试。

### C2：完全信息 MCTS（已完成，2026-09-15）

- [x] 先供剧作家 AI 与调试基准使用，并在局域网大厅作为独立 AI 类型提供；
- [x] 使用后端 `action_offers` / `transition`，不解析界面文本；
- [x] 将主人公视为对抗方，在 UCT 中最小化剧作家收益；随机 rollout 覆盖阶段内能力顺序与规则集扩展行动。

当前大厅 ISMCTS 预算为至少 96 次根行动访问、24 个粒子、名义 rollout 深度 16；根分支更多时每个合法根行动仍至少访问一次。
rollout 不会在当天中途估值，而会继续到当前天的 `day_ended`，或到立即失败形成的轮回边界。命令行基准可自行扩大。墙钟限制仅作为超时保险，要求逐次完全复现时应只
使用固定节点预算。MCTS 不向主人公开放完整状态；后续 C3 将用与观察一致的隐藏世界采样替换根状态来源。
性能剖析、无损优化与尚未实施的剪枝候选见 [MCTS 性能诊断与后续优化计划](mcts-performance-plan.md)。

朴素 `mcts_mastermind` 始终保留为可在大厅选择和基准工具中对照的基线；新增
`optimized_mcts_mastermind` 使用轻量搜索副本、内部原始命令、强制行动快进、渐进拓宽及可审计的行动先验。
第二批优化增加实际落子后的匹配子树复用，并提供八规则集统一延迟/内存矩阵基准；Trace 会记录复用节点数，仍只存在
服务端进程内。

第三个独立版本 `strategic_mcts_mastermind` 已加入剧本路线扩展先验和剧作家策略 rollout，并在 FS/BTX 65 局低预算基准中
达到 95.4%。旧朴素/优化模式继续保留。下一步先增强主人公公开信息策略（事件防守、关键角色保护、好感能力），再重跑同一矩阵。

`defensive_protagonist` 已完成上述确定性防守并作为独立房间选项保留；同矩阵下剧作家仍为 61/65（93.8%），仅少一胜且
平均对局明显变长。下一主人公版本应根据公开揭牌历史估计剧作家关注目标，形成不读取真实 setup 的轻量风险分布。

轻量历史风险版已经验证但未通过强度门槛：FS 预检中剧作家 10/10，高于防守版的 7/10。结果作为负基线保留；C3 的下一步
改为完成房间级粒子增量维护、命令可见性和信息集行动聚合，避免把单一行为模式误当作身份事实。

### C2.5：剧本条件化局面估值（进行中；FS/BTX 第一版已完成）

当前 `MastermindEvaluator` 虽然接收完整 `game`，能够读取剧本设置和当前状态，但只是跨剧本共用的线性启发式；在多数
rollout 无法到达终局时，它实际承担了主要棋力判断，因此需要升级为显式的 `V(setup, state, perspective)`：

- [x] 将剧本静态设置预编译为 `ScenarioEvaluationContext`：规则 X/Y、身份配置、事件日程、失败路线、角色阈值和特殊规则；
- [x] 将当前局面提取为与规则实现解耦的 `PositionFeatures`：各失败路线进度、距离截止日、行动资源、角色存活、公开知识和
  当前结算时间点；
- [x] 为 FS/BTX 的主规则、身份路线、事件截止和规则 X 建立可组合贡献；其他规则集暂时明确回退旧估值；
- [x] 对 FS/BTX 阈值型效果使用非线性“悬崖”评分；路线依赖与跨路线概率校准仍待深化；
- [x] 标记稳定结算时间点与尚未翻牌/尚未完成强制能力的中间状态，为后续稳定边界 rollout 提供依据；
- [x] 输出私有、可审计的贡献明细，说明失败路线、截止日、轮回进度和主人公资源；公开知识只进入信念/猜测层，不作为行动估值奖励；
- [ ] 完全信息 MCTS 在真实 setup 上评价；ISMCTS 必须分别在每个采样 setup 上评价后按粒子权重聚合，不能读取真实 setup；
- [ ] 用更深搜索或完整终局样本校准分数与胜率的关系，并按剧本留出测试；暂不直接采用需要大量对局数据的神经网络价值模型；
- [x] 为 FS/BTX 建立终局精确性、局部单调性、跨阶段标记和规则集特例测试；
- [ ] 增加深搜索回报相关性、胜率校准与特征组消融测试，再把当前权重视为稳定版本；

FS/BTX 第一版已接入 `optimized_mcts_mastermind`，朴素 MCTS 仍使用旧估值作为固定基线。自动测试覆盖全部 13 个已录入
FS/BTX 剧本的逐步评价直至真实终局，并另行覆盖未录入的 FS“复仇火种/最黑暗剧本”、五种主规则阈值、角色路线、
事件过期、轮回进度与中间阶段。公开知识仍作为状态特征保留，但不再直接增加行动价值。当前仍属于规则驱动初值，尚未用深搜索
回报完成胜率校准。

推荐接口是“setup 编译一次、state 高频评价”：`compile(script, ruleset) -> evaluator(state, perspective)`。终局仍严格返回
`±1`；非终局分数解释为经校准的剧作家胜势，范围保持有界。主人公的信息价值和探索奖励应放在 ISMCTS 选择策略中，
不与真实局面胜率混成同一个不可解释的分数。

### C3：ISMCTS

- [x] 定义只从主人公投影构造的 `PublicEvidence`，忽略剧本 ID、标题和未揭示秘密；
- [x] 增加目录粒子采样器，并按公开登场角色、事件日程、已知身份/当事人/规则过滤；候选为空时不回退真实剧本；
- [x] 按规则 X/Y 身份槽、角色数量上限与事件约束生成组合粒子，并由严格剧本校验器逐个验收；
- [x] 将标准粒子物化为真实游戏，并用合法公开命令重放和语言无关的最终公开快照淘汰矛盾粒子；
- [x] 对未公开行动枚举粒子内合法后继，仅保留公开快照一致的行动；观测等价的暗牌不会被错误区分；
- [x] 真实引擎在每次决策后为各主人公记录语义快照摘要；摘要不含命令，模拟/搜索副本不会追加真实观察历史；
- [x] 粒子过滤器可按观察摘要推进多个隐藏世界，按完整私密状态去重，并用固定种子蓄水池采样限制宽分支规模；
- [x] 定义硬/软 witness 的信息边界、三值匹配和规则集扩展纪律；FS/BTX 第一批硬 witness 覆盖公开身份、当事人、规则与事件发生阈值；
- [x] FS/BTX 从公开移动/死亡/失败日志生成带通道上限的软 witness，并保留不解释现象的候选粒子；
- [x] 将证据作为独立席位状态：只过滤/加权可能世界并服务最终猜测，不占 MCTS 节点预算、不直接选择行动，也不奖励故意失败取证；
- [x] 两人局团队 AI 从整局观察检查点增量维护一份共享粒子集合；团队自己的三张牌可见，剧作家牌只用公开字段缩小分支；
- [x] 每个席位的命令观察显式标为 `public / partial / hidden`；未翻开的他人行动牌只保留公开目标并枚举牌面，不复制完整 `history`；
- [ ] 将规则结算 Observation 约束接入增量粒子更新，而不只在重放末尾比较完整快照；
- [x] FS/BTX 第一版在根信息集聚合不同隐藏世界的访问、收益与行动 availability，并接入主人公 ISMCTS 房间选项；
- [x] 两人局由一个团队 ISMCTS 实例控制 A/B/C，并完成三张主人公行动牌作为一个联合节点的第一版工程闭环；当前只有第一张牌由根搜索提出，后两张仍由公开信息防守策略补全，尚不等于完整的三牌联合搜索；三主人公独立 AI 留作后续模式；
- [ ] 用固定候选池和跨粒子重复评估替换“第一张搜索、后两张启发式补全”，完成真正的三牌联合行动搜索；
- [ ] 把根信息集搜索扩展为跨阶段树，并用观察日志可靠重建能力/事件内部选择；第一版在这些阶段回退公开信息防守策略；
- [ ] 使用公开现象、未出现的强制效果及对手行动更新粒子权重和可行世界集合。

Witness 的详细设计、保守推断原则和其他规则集的扩展风险见
[ISMCTS 与 witness 证据设计](ismcts-witness-design.md)。第一版明确不把可选能力未发动或玩家行动偏好当成硬事实。

主人公不能访问真实身份、真实规则、当事人或内部因果 Trace。目录采样主要作为信息边界与已知剧本回归工具；正式搜索
使用不查询目录 ID 的组合粒子。当前组合生成覆盖普通角色配置；需要剧本专属角色设置的粒子将在公开设置证据结构化后补齐。

#### C3.1：团队联合行动与跨轮回信念修正（进行中）

2026-09-18 在 `official-fs-01-first-script` 上进行定点诊断。对手固定为 `strategic_mcts_mastermind`（`24×12`），
主人公使用团队 ISMCTS（rollout 深度 16）：

| 主人公根预算 | seed 0–4 主人公胜局 | 观察 |
| ---: | ---: | --- |
| 96 | 0/5 | 每天产生约 63–93 个不同三牌组合，绝大多数组合只评估 1 次、最多 2 次 |
| 384 | 0/5 | 典型日产生约 240 个组合，其中超过半数仍只评估 1 次、最多 4–5 次 |

seed 0 的第一轮在第 4 天因女学生死亡而失败；第二、第三轮剧作家都在第 1 天把女学生移动到巫女所在的神社，
主人公连续选择友好牌而没有禁止或反向移动，随后女学生被杀人狂杀死。它不是逐字重复同一组牌，但重复了同一种未防守的
失败路线。把预算扩大四倍没有改变结果，说明首要问题是候选生成、访问分配与信念连续性，而不是简单增加节点数。

当前第二、第三张牌由 `DefensiveProtagonistAgent` 贪心补全。该策略只使用公开确认的身份/当事人与角色好感能力阈值；
例如女学生或男学生从友好 0 到 2 会立刻解锁降低不安能力，因此 `友好 +2` 得分显著高于身份尚未公开时的“禁止移动”。
它适合作为可复现基线或 rollout 策略之一，不应决定联合行动中的固定两张牌，也不能代替对完整三牌组合的搜索。

按以下顺序实施，每一步独立记录基准并提交：

1. **诊断与基线固化**
   - 在私有搜索 Trace/benchmark JSON 中记录每日候选组合数、每个组合访问数、使用的粒子批次、信念来源、粒子耗尽原因、
     选中组合以及角色边缘概率；不得写入玩家可见日志或 replay。
   - 保存 FS01 seed 0 的逐日回归 trace，并保留 `96×16`、`384×16` 两组基线。证据维护耗时继续与搜索预算、搜索耗时分开。
2. **真正的三牌候选生成**
   - 一次生成完整且合法的三张主人公行动牌组合，而不是只枚举第一张；候选必须覆盖移动、禁止移动、禁止密谋、不安控制、
     好感推进及它们的协同组合。
   - 先建立有上限、可复现且多样的固定候选池；公开信息防守策略可以贡献少量先验候选，但不能垄断第二、第三张牌。
   - 不增加“看到某种死亡就固定出某张牌”的 witness-to-action 捷径。证据通过粒子后验和组合价值影响选择。
3. **跨粒子公平比较与预算分配**
   - 使用共同粒子批次轮流评估候选，使不同组合面对相同或配对的可能世界，减少单次幸运粒子造成的排序噪声。
   - 在加入新候选前保证已有候选获得最低重复访问数；随后再以渐进拓宽增加组合。节点预算只支付模拟，不支付 witness 编译、
     粒子过滤或重采样。
   - 根统计按“同一个完整三牌组合”聚合，不能因为启发式补全中的随机差异不断产生新键。
4. **身份、当事人、暗牌分维信念**
   - 身份与剧本规则保持完整合法的角色分配候选；逐日事件当事人各自维护候选集合；当天未翻开的剧作家行动牌按公开手牌与目标临时抽样。
   - 每次更新依据累计公开 witness 重新评分身份候选，补充并保留多样性；事件发生条件及已公开身份/当事人作为各维约束。只在模拟时临时组合成合法完整世界。
   - 不再让暗牌翻开或单次事件结算淘汰跨轮回身份候选。证据维度独立于搜索节点预算，禁止重复累计同一证据的分数。
   - 对可分解的现象分别作用于身份与当事人；解释本身尚不明确时保留多种可能，避免把某一种解释错误升级为硬事实。
5. **FS/BTX 死亡因果 witness 修正**
   - 日末同区域死亡不能只把同伴解释为杀人狂；必须同时容纳杀手密谋阈值及其他合法死亡来源。
   - witness 保存推断所需的公开死亡时快照，例如位置、存活同伴和公开计数物；仍使用三值判断与软权重，不把多解现象升级为硬结论。
   - 重复相容现象应逐步集中后验；相互冲突的合法解释必须继续共存，直到新的公开事实将其排除。
6. **rollout 与估值对齐**
   - `DefensiveProtagonistAgent` 保留为独立基线，并可作为 rollout 混合策略之一；联合根节点不再由它补全两张牌。
   - 每个候选至少结算到当天稳定边界；粒子使用对应 setup 的 FS/BTX 条件化估值。跨轮回信息价值以后由真实终局回报覆盖，
     本阶段仍不奖励故意输掉轮回来取证。

验收条件：

- 固定预算足够时，每个进入最终比较的候选必须获得多粒子重复访问；Trace 不再出现“预算越大，几乎只增加一次访问的新组合”的现象；
- 构造测试证明三张牌的协同组合能够被生成、聚合和选中，且后两张不依赖固定防守策略；
- 身份和当事人维度可从公开证据独立补充，暗牌只按当前公开牌堆采样；空候选原因可审计，同一公开输入和种子完全复现；
- FS01 重复死亡样本中，女学生为关键人物、巫女为杀人狂等相容假设会随证据增强，但杀手等替代解释不会被错误硬排除；
- 在同一策略剧作家、seed 0–4、主人公 `96×16` 条件下，暂定质量门槛为主人公至少 3/5；若未达到，不以运行时间或单局幸存天数代替胜率验收，而是继续检查失败 trace；
- 通过全量 Python/Web 测试、FS/BTX 已录入剧本合法终局回归及信息边界测试。朴素、公开信息防守和旧 ISMCTS 基线继续保留用于对照。

2026-09-19 进度：完成固定上限的多样化三牌候选池、共同粒子批次的重复访问、私有 benchmark 逐日根统计，
以及根据**已公开**翻牌结果桥接暗牌粒子、在首轮事件发生时扩展尚未公开的当事人候选。多人同区域死亡不再误记为
杀人狂“单独相处”证据；密谋达到阈值时，杀手造成关键人物死亡作为另一条软解释保留。FS01 seed 0 的持久粒子从
原先翻牌时（第 1 天）耗尽，延长到第 3 天；仍会在后续结算耗尽，跨轮回完整历史条件重采样尚未完成。固定
`96×16`、相同 5 个种子的主人公胜局为 1/5（原 0/5），未达到 3/5 的质量门槛。后续分维信念已替代新版 AI 的整局持久粒子，但候选覆盖和跨日风险估值仍需改进；不能把本切片标为 C3.1 完成。旧版第一张搜索/后两张启发式补全模式保留为
`ismcts_legacy_protagonist`，可在大厅及自对弈基准中与新版并列选择。

2026-09-19 信念架构改为三维：身份/剧本规则候选、每一天的当事人候选、当前暗牌候选。先在 FS/BTX 上完成分维证据评分、合法世界组合与固定种子自对弈，再检查 FS01 的 3/5 质量门槛。
**低优先级 TODO：** 增加确有必要的跨维度关联因子，例如同一现象存在多个互斥因果解释、身份改变事件目标或多事件共享约束时；先用构造案例证明独立维度不足，再增加联合评分。还需为历史事件保存结算时快照，避免事后用当前棋盘推断旧日当事人。

本次实现：FS/BTX 的新版团队 AI 已分别维护身份/剧本候选、每日事件当事人候选和当前暗牌；6 人及以下完整枚举合法身份分配，其他剧本使用可补充的有界候选池。FS 中单人死亡紧接轮回失败时，公开结算可确定其为关键人物；低密谋角色在日末与唯一同伴同区死亡时，该同伴可确定为杀人狂。两者均形成硬 witness。BTX 由于不安定因子可能获得关键人物能力，暂不套用同一身份断言。FS01 seed 1 在第二轮后的 24/24 个模拟世界中，女学生均为关键人物。

固定策略剧作家 `24×12`、FS01 seed 0–4 的主人公胜局：`96×16` 为 2/5，`384×24`（64 个预采样组合世界）也为 2/5；后者平均整局耗时约 42 秒。高预算已接入房间新版团队 AI，便于试玩与进一步诊断，但 **3/5 质量门槛仍未达到**。384 次模拟下把候选上限从 24 缩到 12 仍为 2/5、平均约 52 秒，因此保留 24 候选。下一优先级是跨日保留关键防守牌、未来剧作家行动的合理模拟，以及候选池对关键保护动作的覆盖与配对比较；逐项跑固定种子消融。身份/当事人跨维度关联维持低优先级。

2026-09-20 新增并保留独立可选的“当日生存优先 ISMCTS”，与上述旧模式对照。其三牌候选加入禁止移动关键人物、改变关键人物目的地、移动其他角色避免与杀人狂单独相处；历史揭牌提高同目标暗牌抽样权重并保留均匀抽样。当日结算后即停，优先比较存活率。FS01 同样 5 个固定种子、`96×16` 主人公预算达到 4/5（平均约 20.6 秒），因此**此实验模式**通过 3/5 小样本门槛；旧模式仍为 2/5。不要把每组 8 次采样的“全活”解释成对所有合法暗牌的证明。下一步应扩充 BTX 和更多 FS 剧本对弈、检查跨日使用三名主人公各自的限次牌，并实现真正的敌对抽样／安全性证书，而非只依赖历史加权的随机样本。

随后遵照 FS 优先，在 `silent-town-fs` 和 `official-fs-02-prevailing-secrecy` 各做 seed 0–2 的同预算对照：前者两版均 3/3，后者两版均 0/3。FS02 的 seed 0 四轮均由医生死亡**立即**失败，单人死亡轮次已有硬 witness 能推出医生是关键人物，但行动搜索仍反复没挡住后续医院/远隔事件；这才是该样本的直接短板。另用纯学校密谋导致的正常轮回末失败诊断局，独立确认 `loop_lost` 尚无公开版图状态驱动的规则 X/Y witness。后者应保存**轮回末公开状态快照**并做多原因相容性/软打分测试；若同时有关键人物死亡，绝不可把规则 Y 单独硬确定。下一阶段分别改进跨日事件防御、轮回末证据和长剧本延迟。BTX 的广泛对弈暂缓，不把 FS01 的战术提升泛化到其它剧本。

### C4：带玩家猜测模型的信念 AI

- 显式维护身份、规则和事件当事人的概率分布；
- 区分规则必然现象、玩家可选行为与玩家失误，避免把“没有发动”当作硬证据；
- 为其他玩家建立嵌套但有界的信念/策略模型；
- 可选择不同风险偏好和玩家熟练度模型，并输出可审计的决策依据。

ISMCTS 和信念更新必须只使用该座位可见的结构化 Observation。若需要改变核心协议，先增加架构测试，
不得通过读取完整剧作家状态绕过信息集问题。

## 阶段 D：远程联机与 ECS 部署

需要用户提供或授权使用一台公网 ECS 实例。第一版沿用现有房间、令牌、SSE 和服务端权威结算，增加：

- Linux 服务进程、反向代理、HTTPS、域名和自动重启；
- 防火墙/安全组只开放 HTTPS 和必要管理端口；
- 房间限流、请求体限制、日志轮转、健康检查和基本备份；
- SSE 经过代理时的缓冲关闭、空闲超时和断线重连验证；
- 部署配置与密钥分离，仓库中只保存模板；
- 手机公网访问、刷新恢复、长局断线和版本升级的人工验收。

计算量较低时，小规格 ECS 足以支撑少量私人房间；启用大量 MCTS 后，需要把 AI 任务并发、思考预算和
资源上限纳入容量评估。暂不建设公共账号、匹配、聊天或对抗恶意作弊系统。

## 提交与验收纪律

- 阶段 A 可按机制相近的角色分批提交，每批保持可运行；
- 官方剧本按来源批次提交，数据与引擎修复分开；
- AI 的搜索器、信念模型和策略调整分别提交并保留基准数据；
- 云部署配置不得包含令牌、私钥或真实密码；
- 每个纵向切片必须通过 Python 全量测试、Web 测试和生产构建。
